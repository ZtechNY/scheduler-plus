# Test-only dependencies for Scheduler+.
#
# pytest-homeassistant-custom-component bundles the Home Assistant core test
# harness (the `hass` fixture, MockConfigEntry, config entry setup helpers)
# and pins its own compatible pytest / pytest-asyncio versions internally,
# so those are not listed separately here.
#
# TODO: pin an exact version once CI is set up, matching the minimum
# Home Assistant version this integration targets (see the "homeassistant"
# key in hacs.json).
pytest-homeassistant-custom-component

# report_view.py's runtime dependency (also declared in manifest.json's
# requirements, which is how a real Home Assistant install gets it - listed
# here too so the test environment has it as well).
fpdf2>=2.7,<3

{
  "config": {
    "step": {
      "user": {
        "title": "Add Scheduler+",
        "description": "Scheduler+ is a visual scheduling engine for lights and climate devices. Individual schedules are created afterwards from the Scheduler+ dashboard."
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Default scheduling preferences",
        "description": "The fallback used by the rule editor's quick-fill presets (Weekdays, Weekend, After hours) for anyone who hasn't set their own under My Preferences on the Scheduler+ card. This does not override a user's own choice.",
        "data": {
          "weekday_days": "Weekday days",
          "weekend_days": "Weekend days",
          "working_hours_start": "Working hours start",
          "working_hours_end": "Working hours end"
        }
      }
    }
  },
  "entity": {
    "binary_sensor": {
      "schedule_enabled": {
        "name": "Enabled"
      }
    },
    "sensor": {
      "schedule_next_event": {
        "name": "Next event"
      }
    }
  }
}

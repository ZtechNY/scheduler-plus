var Y2=Object.defineProperty;var X2=Object.getOwnPropertyDescriptor;var r=(L,V,C,H)=>{for(var M=H>1?void 0:H?X2(V,C):V,e=L.length-1,i;e>=0;e--)(i=L[e])&&(M=(H?i(V,C,M):i(M))||M);return H&&M&&Y2(V,C,M),M};var M2="M10.63,14.1C12.23,10.58 16.38,9.03 19.9,10.63C23.42,12.23 24.97,16.38 23.37,19.9C22.24,22.4 19.75,24 17,24C14.3,24 11.83,22.44 10.67,20H1V18C1.06,16.86 1.84,15.93 3.34,15.18C4.84,14.43 6.72,14.04 9,14C9.57,14 10.11,14.05 10.63,14.1V14.1M9,4C10.12,4.03 11.06,4.42 11.81,5.17C12.56,5.92 12.93,6.86 12.93,8C12.93,9.14 12.56,10.08 11.81,10.83C11.06,11.58 10.12,11.95 9,11.95C7.88,11.95 6.94,11.58 6.19,10.83C5.44,10.08 5.07,9.14 5.07,8C5.07,6.86 5.44,5.92 6.19,5.17C6.94,4.42 7.88,4.03 9,4M17,22A5,5 0 0,0 22,17A5,5 0 0,0 17,12A5,5 0 0,0 12,17A5,5 0 0,0 17,22M16,14H17.5V16.82L19.94,18.23L19.19,19.53L16,17.69V14Z";var e2="M15,13H16.5V15.82L18.94,17.23L18.19,18.53L15,16.69V13M19,8H5V19H9.67C9.24,18.09 9,17.07 9,16A7,7 0 0,1 16,9C17.07,9 18.09,9.24 19,9.67V8M5,21C3.89,21 3,20.1 3,19V5C3,3.89 3.89,3 5,3H6V1H8V3H16V1H18V3H19A2,2 0 0,1 21,5V11.1C22.24,12.36 23,14.09 23,16A7,7 0 0,1 16,23C14.09,23 12.36,22.24 11.1,21H5M16,11.15A4.85,4.85 0 0,0 11.15,16C11.15,18.68 13.32,20.85 16,20.85A4.85,4.85 0 0,0 20.85,16C20.85,13.32 18.68,11.15 16,11.15Z";var r2="M19 19V8H5V19H19M16 1H18V3H19C20.11 3 21 3.9 21 5V19C21 20.11 20.11 21 19 21H5C3.89 21 3 20.1 3 19V5C3 3.89 3.89 3 5 3H6V1H8V3H16V1M11 9.5H13V12.5H16V14.5H13V17.5H11V14.5H8V12.5H11V9.5Z";var t2="M19,21H8V7H19M19,5H8A2,2 0 0,0 6,7V21A2,2 0 0,0 8,23H19A2,2 0 0,0 21,21V7A2,2 0 0,0 19,5M16,1H4A2,2 0 0,0 2,3V17H4V3H16V1Z";var E="M19,4H15.5L14.5,3H9.5L8.5,4H5V6H19M6,19A2,2 0 0,0 8,21H16A2,2 0 0,0 18,19V7H6V19Z";var i2="M13,16V8H15V16H13M9,16V8H11V16H9M12,2A10,10 0 0,1 22,12A10,10 0 0,1 12,22A10,10 0 0,1 2,12A10,10 0 0,1 12,2M12,4A8,8 0 0,0 4,12A8,8 0 0,0 12,20A8,8 0 0,0 20,12A8,8 0 0,0 12,4Z";var x1="M20.71,7.04C21.1,6.65 21.1,6 20.71,5.63L18.37,3.29C18,2.9 17.35,2.9 16.96,3.29L15.12,5.12L18.87,8.87M3,17.25V21H6.75L17.81,9.93L14.06,6.18L3,17.25Z";var a2="M12,20C7.59,20 4,16.41 4,12C4,7.59 7.59,4 12,4C16.41,4 20,7.59 20,12C20,16.41 16.41,20 12,20M12,2A10,10 0 0,0 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12A10,10 0 0,0 12,2M10,16.5L16,12L10,7.5V16.5Z";var o2="M3 21H11V13H3M5 15H9V19H5M3 11H11V3H3M5 5H9V9H5M13 3V11H21V3M19 9H15V5H19M18 16H21V18H18V21H16V18H13V16H16V13H18Z";var s1=globalThis,Z1=s1.ShadowRoot&&(s1.ShadyCSS===void 0||s1.ShadyCSS.nativeShadow)&&"adoptedStyleSheets"in Document.prototype&&"replace"in CSSStyleSheet.prototype,$1=Symbol(),A2=new WeakMap,i1=class{constructor(V,C,H){if(this._$cssResult$=!0,H!==$1)throw Error("CSSResult is not constructable. Use `unsafeCSS` or `css` instead.");this.cssText=V,this.t=C}get styleSheet(){let V=this.o,C=this.t;if(Z1&&V===void 0){let H=C!==void 0&&C.length===1;H&&(V=A2.get(C)),V===void 0&&((this.o=V=new CSSStyleSheet).replaceSync(this.cssText),H&&A2.set(C,V))}return V}toString(){return this.cssText}},d2=L=>new i1(typeof L=="string"?L:L+"",void 0,$1),v=(L,...V)=>{let C=L.length===1?L[0]:V.reduce((H,M,e)=>H+(i=>{if(i._$cssResult$===!0)return i.cssText;if(typeof i=="number")return i;throw Error("Value passed to 'css' function must be a 'css' function result: "+i+". Use 'unsafeCSS' to pass non-literal values, but take care to ensure page security.")})(M)+L[e+1],L[0]);return new i1(C,L,$1)},p2=(L,V)=>{if(Z1)L.adoptedStyleSheets=V.map(C=>C instanceof CSSStyleSheet?C:C.styleSheet);else for(let C of V){let H=document.createElement("style"),M=s1.litNonce;M!==void 0&&H.setAttribute("nonce",M),H.textContent=C.cssText,L.appendChild(H)}},N1=Z1?L=>L:L=>L instanceof CSSStyleSheet?(V=>{let C="";for(let H of V.cssRules)C+=H.cssText;return d2(C)})(L):L;var{is:J2,defineProperty:C5,getOwnPropertyDescriptor:H5,getOwnPropertyNames:V5,getOwnPropertySymbols:L5,getPrototypeOf:M5}=Object,c1=globalThis,n2=c1.trustedTypes,e5=n2?n2.emptyScript:"",r5=c1.reactiveElementPolyfillSupport,a1=(L,V)=>L,o1={toAttribute(L,V){switch(V){case Boolean:L=L?e5:null;break;case Object:case Array:L=L==null?L:JSON.stringify(L)}return L},fromAttribute(L,V){let C=L;switch(V){case Boolean:C=L!==null;break;case Number:C=L===null?null:Number(L);break;case Object:case Array:try{C=JSON.parse(L)}catch{C=null}}return C}},u1=(L,V)=>!J2(L,V),l2={attribute:!0,type:String,converter:o1,reflect:!1,useDefault:!1,hasChanged:u1};Symbol.metadata??=Symbol("metadata"),c1.litPropertyMetadata??=new WeakMap;var R=class extends HTMLElement{static addInitializer(V){this._$Ei(),(this.l??=[]).push(V)}static get observedAttributes(){return this.finalize(),this._$Eh&&[...this._$Eh.keys()]}static createProperty(V,C=l2){if(C.state&&(C.attribute=!1),this._$Ei(),this.prototype.hasOwnProperty(V)&&((C=Object.create(C)).wrapped=!0),this.elementProperties.set(V,C),!C.noAccessor){let H=Symbol(),M=this.getPropertyDescriptor(V,H,C);M!==void 0&&C5(this.prototype,V,M)}}static getPropertyDescriptor(V,C,H){let{get:M,set:e}=H5(this.prototype,V)??{get(){return this[C]},set(i){this[C]=i}};return{get:M,set(i){let d=M?.call(this);e?.call(this,i),this.requestUpdate(V,d,H)},configurable:!0,enumerable:!0}}static getPropertyOptions(V){return this.elementProperties.get(V)??l2}static _$Ei(){if(this.hasOwnProperty(a1("elementProperties")))return;let V=M5(this);V.finalize(),V.l!==void 0&&(this.l=[...V.l]),this.elementProperties=new Map(V.elementProperties)}static finalize(){if(this.hasOwnProperty(a1("finalized")))return;if(this.finalized=!0,this._$Ei(),this.hasOwnProperty(a1("properties"))){let C=this.properties,H=[...V5(C),...L5(C)];for(let M of H)this.createProperty(M,C[M])}let V=this[Symbol.metadata];if(V!==null){let C=litPropertyMetadata.get(V);if(C!==void 0)for(let[H,M]of C)this.elementProperties.set(H,M)}this._$Eh=new Map;for(let[C,H]of this.elementProperties){let M=this._$Eu(C,H);M!==void 0&&this._$Eh.set(M,C)}this.elementStyles=this.finalizeStyles(this.styles)}static finalizeStyles(V){let C=[];if(Array.isArray(V)){let H=new Set(V.flat(1/0).reverse());for(let M of H)C.unshift(N1(M))}else V!==void 0&&C.push(N1(V));return C}static _$Eu(V,C){let H=C.attribute;return H===!1?void 0:typeof H=="string"?H:typeof V=="string"?V.toLowerCase():void 0}constructor(){super(),this._$Ep=void 0,this.isUpdatePending=!1,this.hasUpdated=!1,this._$Em=null,this._$Ev()}_$Ev(){this._$ES=new Promise(V=>this.enableUpdating=V),this._$AL=new Map,this._$E_(),this.requestUpdate(),this.constructor.l?.forEach(V=>V(this))}addController(V){(this._$EO??=new Set).add(V),this.renderRoot!==void 0&&this.isConnected&&V.hostConnected?.()}removeController(V){this._$EO?.delete(V)}_$E_(){let V=new Map,C=this.constructor.elementProperties;for(let H of C.keys())this.hasOwnProperty(H)&&(V.set(H,this[H]),delete this[H]);V.size>0&&(this._$Ep=V)}createRenderRoot(){let V=this.shadowRoot??this.attachShadow(this.constructor.shadowRootOptions);return p2(V,this.constructor.elementStyles),V}connectedCallback(){this.renderRoot??=this.createRenderRoot(),this.enableUpdating(!0),this._$EO?.forEach(V=>V.hostConnected?.())}enableUpdating(V){}disconnectedCallback(){this._$EO?.forEach(V=>V.hostDisconnected?.())}attributeChangedCallback(V,C,H){this._$AK(V,H)}_$ET(V,C){let H=this.constructor.elementProperties.get(V),M=this.constructor._$Eu(V,H);if(M!==void 0&&H.reflect===!0){let e=(H.converter?.toAttribute!==void 0?H.converter:o1).toAttribute(C,H.type);this._$Em=V,e==null?this.removeAttribute(M):this.setAttribute(M,e),this._$Em=null}}_$AK(V,C){let H=this.constructor,M=H._$Eh.get(V);if(M!==void 0&&this._$Em!==M){let e=H.getPropertyOptions(M),i=typeof e.converter=="function"?{fromAttribute:e.converter}:e.converter?.fromAttribute!==void 0?e.converter:o1;this._$Em=M;let d=i.fromAttribute(C,e.type);this[M]=d??this._$Ej?.get(M)??d,this._$Em=null}}requestUpdate(V,C,H,M=!1,e){if(V!==void 0){let i=this.constructor;if(M===!1&&(e=this[V]),H??=i.getPropertyOptions(V),!((H.hasChanged??u1)(e,C)||H.useDefault&&H.reflect&&e===this._$Ej?.get(V)&&!this.hasAttribute(i._$Eu(V,H))))return;this.C(V,C,H)}this.isUpdatePending===!1&&(this._$ES=this._$EP())}C(V,C,{useDefault:H,reflect:M,wrapped:e},i){H&&!(this._$Ej??=new Map).has(V)&&(this._$Ej.set(V,i??C??this[V]),e!==!0||i!==void 0)||(this._$AL.has(V)||(this.hasUpdated||H||(C=void 0),this._$AL.set(V,C)),M===!0&&this._$Em!==V&&(this._$Eq??=new Set).add(V))}async _$EP(){this.isUpdatePending=!0;try{await this._$ES}catch(C){Promise.reject(C)}let V=this.scheduleUpdate();return V!=null&&await V,!this.isUpdatePending}scheduleUpdate(){return this.performUpdate()}performUpdate(){if(!this.isUpdatePending)return;if(!this.hasUpdated){if(this.renderRoot??=this.createRenderRoot(),this._$Ep){for(let[M,e]of this._$Ep)this[M]=e;this._$Ep=void 0}let H=this.constructor.elementProperties;if(H.size>0)for(let[M,e]of H){let{wrapped:i}=e,d=this[M];i!==!0||this._$AL.has(M)||d===void 0||this.C(M,void 0,e,d)}}let V=!1,C=this._$AL;try{V=this.shouldUpdate(C),V?(this.willUpdate(C),this._$EO?.forEach(H=>H.hostUpdate?.()),this.update(C)):this._$EM()}catch(H){throw V=!1,this._$EM(),H}V&&this._$AE(C)}willUpdate(V){}_$AE(V){this._$EO?.forEach(C=>C.hostUpdated?.()),this.hasUpdated||(this.hasUpdated=!0,this.firstUpdated(V)),this.updated(V)}_$EM(){this._$AL=new Map,this.isUpdatePending=!1}get updateComplete(){return this.getUpdateComplete()}getUpdateComplete(){return this._$ES}shouldUpdate(V){return!0}update(V){this._$Eq&&=this._$Eq.forEach(C=>this._$ET(C,this[C])),this._$EM()}updated(V){}firstUpdated(V){}};R.elementStyles=[],R.shadowRootOptions={mode:"open"},R[a1("elementProperties")]=new Map,R[a1("finalized")]=new Map,r5?.({ReactiveElement:R}),(c1.reactiveElementVersions??=[]).push("2.1.2");var K1=globalThis,m2=L=>L,S1=K1.trustedTypes,v2=S1?S1.createPolicy("lit-html",{createHTML:L=>L}):void 0,S2="$lit$",$=`lit$${Math.random().toFixed(9).slice(2)}$`,h2="?"+$,t5=`<${h2}>`,U=document,d1=()=>U.createComment(""),p1=L=>L===null||typeof L!="object"&&typeof L!="function",q1=Array.isArray,i5=L=>q1(L)||typeof L?.[Symbol.iterator]=="function",I1=`[ 	
\f\r]`,A1=/<(?:(!--|\/[^a-zA-Z])|(\/?[a-zA-Z][^>\s]*)|(\/?$))/g,x2=/-->/g,s2=/>/g,I=RegExp(`>|${I1}(?:([^\\s"'>=/]+)(${I1}*=${I1}*(?:[^ 	
\f\r"'\`<>=]|("|')|))|$)`,"g"),Z2=/'/g,c2=/"/g,g2=/^(?:script|style|textarea|title)$/i,j1=L=>(V,...C)=>({_$litType$:L,strings:V,values:C}),t=j1(1),H1=j1(2),Q5=j1(3),G=Symbol.for("lit-noChange"),o=Symbol.for("lit-nothing"),u2=new WeakMap,W=U.createTreeWalker(U,129);function f2(L,V){if(!q1(L)||!L.hasOwnProperty("raw"))throw Error("invalid template strings array");return v2!==void 0?v2.createHTML(V):V}var a5=(L,V)=>{let C=L.length-1,H=[],M,e=V===2?"<svg>":V===3?"<math>":"",i=A1;for(let d=0;d<C;d++){let A=L[d],l,Z,s=-1,P=0;for(;P<A.length&&(i.lastIndex=P,Z=i.exec(A),Z!==null);)P=i.lastIndex,i===A1?Z[1]==="!--"?i=x2:Z[1]!==void 0?i=s2:Z[2]!==void 0?(g2.test(Z[2])&&(M=RegExp("</"+Z[2],"g")),i=I):Z[3]!==void 0&&(i=I):i===I?Z[0]===">"?(i=M??A1,s=-1):Z[1]===void 0?s=-2:(s=i.lastIndex-Z[2].length,l=Z[1],i=Z[3]===void 0?I:Z[3]==='"'?c2:Z2):i===c2||i===Z2?i=I:i===x2||i===s2?i=A1:(i=I,M=void 0);let F=i===I&&L[d+1].startsWith("/>")?" ":"";e+=i===A1?A+t5:s>=0?(H.push(l),A.slice(0,s)+S2+A.slice(s)+$+F):A+$+(s===-2?d:F)}return[f2(L,e+(L[C]||"<?>")+(V===2?"</svg>":V===3?"</math>":"")),H]},n1=class L{constructor({strings:V,_$litType$:C},H){let M;this.parts=[];let e=0,i=0,d=V.length-1,A=this.parts,[l,Z]=a5(V,C);if(this.el=L.createElement(l,H),W.currentNode=this.el.content,C===2||C===3){let s=this.el.content.firstChild;s.replaceWith(...s.childNodes)}for(;(M=W.nextNode())!==null&&A.length<d;){if(M.nodeType===1){if(M.hasAttributes())for(let s of M.getAttributeNames())if(s.endsWith(S2)){let P=Z[i++],F=M.getAttribute(s).split($),v1=/([.?@])?(.*)/.exec(P);A.push({type:1,index:e,name:v1[2],strings:F,ctor:v1[1]==="."?U1:v1[1]==="?"?G1:v1[1]==="@"?z1:C1}),M.removeAttribute(s)}else s.startsWith($)&&(A.push({type:6,index:e}),M.removeAttribute(s));if(g2.test(M.tagName)){let s=M.textContent.split($),P=s.length-1;if(P>0){M.textContent=S1?S1.emptyScript:"";for(let F=0;F<P;F++)M.append(s[F],d1()),W.nextNode(),A.push({type:2,index:++e});M.append(s[P],d1())}}}else if(M.nodeType===8)if(M.data===h2)A.push({type:2,index:e});else{let s=-1;for(;(s=M.data.indexOf($,s+1))!==-1;)A.push({type:7,index:e}),s+=$.length-1}e++}}static createElement(V,C){let H=U.createElement("template");return H.innerHTML=V,H}};function J(L,V,C=L,H){if(V===G)return V;let M=H!==void 0?C._$Co?.[H]:C._$Cl,e=p1(V)?void 0:V._$litDirective$;return M?.constructor!==e&&(M?._$AO?.(!1),e===void 0?M=void 0:(M=new e(L),M._$AT(L,C,H)),H!==void 0?(C._$Co??=[])[H]=M:C._$Cl=M),M!==void 0&&(V=J(L,M._$AS(L,V.values),M,H)),V}var W1=class{constructor(V,C){this._$AV=[],this._$AN=void 0,this._$AD=V,this._$AM=C}get parentNode(){return this._$AM.parentNode}get _$AU(){return this._$AM._$AU}u(V){let{el:{content:C},parts:H}=this._$AD,M=(V?.creationScope??U).importNode(C,!0);W.currentNode=M;let e=W.nextNode(),i=0,d=0,A=H[0];for(;A!==void 0;){if(i===A.index){let l;A.type===2?l=new l1(e,e.nextSibling,this,V):A.type===1?l=new A.ctor(e,A.name,A.strings,this,V):A.type===6&&(l=new Q1(e,this,V)),this._$AV.push(l),A=H[++d]}i!==A?.index&&(e=W.nextNode(),i++)}return W.currentNode=U,M}p(V){let C=0;for(let H of this._$AV)H!==void 0&&(H.strings!==void 0?(H._$AI(V,H,C),C+=H.strings.length-2):H._$AI(V[C])),C++}},l1=class L{get _$AU(){return this._$AM?._$AU??this._$Cv}constructor(V,C,H,M){this.type=2,this._$AH=o,this._$AN=void 0,this._$AA=V,this._$AB=C,this._$AM=H,this.options=M,this._$Cv=M?.isConnected??!0}get parentNode(){let V=this._$AA.parentNode,C=this._$AM;return C!==void 0&&V?.nodeType===11&&(V=C.parentNode),V}get startNode(){return this._$AA}get endNode(){return this._$AB}_$AI(V,C=this){V=J(this,V,C),p1(V)?V===o||V==null||V===""?(this._$AH!==o&&this._$AR(),this._$AH=o):V!==this._$AH&&V!==G&&this._(V):V._$litType$!==void 0?this.$(V):V.nodeType!==void 0?this.T(V):i5(V)?this.k(V):this._(V)}O(V){return this._$AA.parentNode.insertBefore(V,this._$AB)}T(V){this._$AH!==V&&(this._$AR(),this._$AH=this.O(V))}_(V){this._$AH!==o&&p1(this._$AH)?this._$AA.nextSibling.data=V:this.T(U.createTextNode(V)),this._$AH=V}$(V){let{values:C,_$litType$:H}=V,M=typeof H=="number"?this._$AC(V):(H.el===void 0&&(H.el=n1.createElement(f2(H.h,H.h[0]),this.options)),H);if(this._$AH?._$AD===M)this._$AH.p(C);else{let e=new W1(M,this),i=e.u(this.options);e.p(C),this.T(i),this._$AH=e}}_$AC(V){let C=u2.get(V.strings);return C===void 0&&u2.set(V.strings,C=new n1(V)),C}k(V){q1(this._$AH)||(this._$AH=[],this._$AR());let C=this._$AH,H,M=0;for(let e of V)M===C.length?C.push(H=new L(this.O(d1()),this.O(d1()),this,this.options)):H=C[M],H._$AI(e),M++;M<C.length&&(this._$AR(H&&H._$AB.nextSibling,M),C.length=M)}_$AR(V=this._$AA.nextSibling,C){for(this._$AP?.(!1,!0,C);V!==this._$AB;){let H=m2(V).nextSibling;m2(V).remove(),V=H}}setConnected(V){this._$AM===void 0&&(this._$Cv=V,this._$AP?.(V))}},C1=class{get tagName(){return this.element.tagName}get _$AU(){return this._$AM._$AU}constructor(V,C,H,M,e){this.type=1,this._$AH=o,this._$AN=void 0,this.element=V,this.name=C,this._$AM=M,this.options=e,H.length>2||H[0]!==""||H[1]!==""?(this._$AH=Array(H.length-1).fill(new String),this.strings=H):this._$AH=o}_$AI(V,C=this,H,M){let e=this.strings,i=!1;if(e===void 0)V=J(this,V,C,0),i=!p1(V)||V!==this._$AH&&V!==G,i&&(this._$AH=V);else{let d=V,A,l;for(V=e[0],A=0;A<e.length-1;A++)l=J(this,d[H+A],C,A),l===G&&(l=this._$AH[A]),i||=!p1(l)||l!==this._$AH[A],l===o?V=o:V!==o&&(V+=(l??"")+e[A+1]),this._$AH[A]=l}i&&!M&&this.j(V)}j(V){V===o?this.element.removeAttribute(this.name):this.element.setAttribute(this.name,V??"")}},U1=class extends C1{constructor(){super(...arguments),this.type=3}j(V){this.element[this.name]=V===o?void 0:V}},G1=class extends C1{constructor(){super(...arguments),this.type=4}j(V){this.element.toggleAttribute(this.name,!!V&&V!==o)}},z1=class extends C1{constructor(V,C,H,M,e){super(V,C,H,M,e),this.type=5}_$AI(V,C=this){if((V=J(this,V,C,0)??o)===G)return;let H=this._$AH,M=V===o&&H!==o||V.capture!==H.capture||V.once!==H.once||V.passive!==H.passive,e=V!==o&&(H===o||M);M&&this.element.removeEventListener(this.name,this,H),e&&this.element.addEventListener(this.name,this,V),this._$AH=V}handleEvent(V){typeof this._$AH=="function"?this._$AH.call(this.options?.host??this.element,V):this._$AH.handleEvent(V)}},Q1=class{constructor(V,C,H){this.element=V,this.type=6,this._$AN=void 0,this._$AM=C,this.options=H}get _$AU(){return this._$AM._$AU}_$AI(V){J(this,V)}};var o5=K1.litHtmlPolyfillSupport;o5?.(n1,l1),(K1.litHtmlVersions??=[]).push("3.3.3");var O2=(L,V,C)=>{let H=C?.renderBefore??V,M=H._$litPart$;if(M===void 0){let e=C?.renderBefore??null;H._$litPart$=M=new l1(V.insertBefore(d1(),e),e,void 0,C??{})}return M._$AI(L),M};var Y1=globalThis,m=class extends R{constructor(){super(...arguments),this.renderOptions={host:this},this._$Do=void 0}createRenderRoot(){let V=super.createRenderRoot();return this.renderOptions.renderBefore??=V.firstChild,V}update(V){let C=this.render();this.hasUpdated||(this.renderOptions.isConnected=this.isConnected),super.update(V),this._$Do=O2(C,this.renderRoot,this.renderOptions)}connectedCallback(){super.connectedCallback(),this._$Do?.setConnected(!0)}disconnectedCallback(){super.disconnectedCallback(),this._$Do?.setConnected(!1)}render(){return G}};m._$litElement$=!0,m.finalized=!0,Y1.litElementHydrateSupport?.({LitElement:m});var A5=Y1.litElementPolyfillSupport;A5?.({LitElement:m});(Y1.litElementVersions??=[]).push("4.2.2");var x=L=>(V,C)=>{C!==void 0?C.addInitializer(()=>{customElements.define(L,V)}):customElements.define(L,V)};var d5={attribute:!0,type:String,converter:o1,reflect:!1,hasChanged:u1},p5=(L=d5,V,C)=>{let{kind:H,metadata:M}=C,e=globalThis.litPropertyMetadata.get(M);if(e===void 0&&globalThis.litPropertyMetadata.set(M,e=new Map),H==="setter"&&((L=Object.create(L)).wrapped=!0),e.set(C.name,L),H==="accessor"){let{name:i}=C;return{set(d){let A=V.get.call(this);V.set.call(this,d),this.requestUpdate(i,A,L,!0,d)},init(d){return d!==void 0&&this.C(i,void 0,L,d),d}}}if(H==="setter"){let{name:i}=C;return function(d){let A=this[i];V.call(this,d),this.requestUpdate(i,A,L,!0,d)}}throw Error("Unsupported decorator location: "+H)};function n(L){return(V,C)=>typeof C=="object"?p5(L,V,C):((H,M,e)=>{let i=M.hasOwnProperty(e);return M.constructor.createProperty(e,H),i?Object.getOwnPropertyDescriptor(M,e):void 0})(L,V,C)}function a(L){return n({...L,state:!0,attribute:!1})}var z=(L,V,C)=>(C.configurable=!0,C.enumerable=!0,Reflect.decorate&&typeof V!="object"&&Object.defineProperty(L,V,C),C);function O(L,V){return(C,H,M)=>{let e=i=>i.renderRoot?.querySelector(L)??null;if(V){let{get:i,set:d}=typeof H=="object"?C:M??(()=>{let A=Symbol();return{get(){return this[A]},set(l){this[A]=l}}})();return z(C,H,{get(){let A=i.call(this);return A===void 0&&(A=e(this),(A!==null||this.hasUpdated)&&d.call(this,A)),A}})}return z(C,H,{get(){return e(this)}})}}var y="scheduler_plus";function Q(L){return{name:L.name,device_type:L.device_type,entities:L.entities,enabled:L.enabled,rules:L.rules,active_date_mode:L.active_date_mode,active_date_ranges:L.active_date_ranges,override_until:L.override_until}}async function h1(L){return(await L.callWS({type:`${y}/list_schedules`})).schedules}async function g1(L,V){return(await L.callWS({type:`${y}/create_schedule`,...V})).schedule}async function D(L,V,C){return(await L.callWS({type:`${y}/update_schedule`,schedule_id:V,...C})).schedule}async function y2(L,V){await L.callWS({type:`${y}/delete_schedule`,schedule_id:V})}async function f1(L){return L.callWS({type:`${y}/get_preferences`})}async function b2(L,V){return L.callWS({type:`${y}/set_preferences`,...V})}async function k2(L,V,C){return(await L.callWS({type:`${y}/get_day_schedule`,date:V,...C?{device_type:C}:{}})).events}async function _2(L,V,C){return(await L.callWS({type:`${y}/get_week_schedule`,start_date:V,...C?{device_type:C}:{}})).days}async function O1(L){return(await L.callWS({type:`${y}/list_templates`})).templates}async function w2(L,V){return(await L.callWS({type:`${y}/create_template`,...V})).template}async function y1(L,V){await L.callWS({type:`${y}/delete_template`,template_id:V})}async function b1(L,V,C){return(await L.callWS({type:`${y}/check_schedule_conflicts`,...V?{schedule_id:V}:{},...C})).conflicts}async function T2(L,V,C,H){return L.callWS({type:`${y}/generate_report`,entities:V,start_date:C,end_date:H})}function B2(L,V,C){return`/api/scheduler_plus/report/pdf?${new URLSearchParams({entities:L.join(","),start:V,end:C}).toString()}`}var P2=["light_switch","climate"],V1={light:"Light",climate:"Climate",switch:"Switch",light_switch:"Lights & Switches"},k1={light:["light"],climate:["climate"],switch:["switch"],light_switch:["light","switch"]},L1=["light","climate","switch"],_1=["heat","cool","heat_cool","auto","dry","fan_only"],M1={heat:"Heat",cool:"Cool",heat_cool:"Heat/Cool",auto:"Auto",dry:"Dry",fan_only:"Fan only"};var K={fixed:"Fixed time",sunrise:"Sunrise",sunset:"Sunset",yidcal:"YidCal"},R2=["candle_lighting","motzei_shabbos"],w1={candle_lighting:"\u05D4\u05D3\u05DC\u05E7\u05D5\u05EA \u05D4\u05E0\u05D9\u05E8\u05D5\u05EA",motzei_shabbos:'\u05DE\u05D5\u05E6\u05E9"\u05E7'},b=["mon","tue","wed","thu","fri","sat","sun"],q={mon:"Monday",tue:"Tuesday",wed:"Wednesday",thu:"Thursday",fri:"Friday",sat:"Saturday",sun:"Sunday"},T1=["always","include","exclude"],D2={always:"Always",include:"Only on these dates",exclude:"Except these dates"},F2=["shabbos","yom_tov","erev_shabbos","erev_yom_tov"],m1={shabbos:"Shabbos",yom_tov:"Yom Tov",erev_shabbos:"Erev Shabbos",erev_yom_tov:"Erev Yom Tov"};var w=class extends m{constructor(){super(...arguments);this._open=!1;this._templates=[];this._loading=!1;this._closeDialog=()=>{this._open=!1};this._useTemplate=C=>{this._open=!1,this.dispatchEvent(new CustomEvent("scheduler-plus-use-template",{detail:{template:C}}))};this._deleteTemplateRow=async C=>{if(window.confirm(`Delete template "${C.name}"?`))try{await y1(this.hass,C.id),await this._load()}catch(H){window.alert(H instanceof Error?H.message:String(H))}}}showDialog(){this._error=void 0,this._open=!0,this._load()}async _load(){this._loading=!0,this._error=void 0;try{let C=await O1(this.hass);this._templates=C.filter(H=>H.scope==="schedule")}catch(C){this._error=C instanceof Error?C.message:String(C)}finally{this._loading=!1}}render(){return this._open?t`
      <ha-dialog open @closed=${this._closeDialog}>
        <div class="form">
          <div class="dialog-title">From template</div>
          ${this._error?t`<div class="error">${this._error}</div>`:o}
          ${this._renderContent()}
          <div class="dialog-actions">
            <button type="button" class="btn" @click=${this._closeDialog}>Close</button>
          </div>
        </div>
      </ha-dialog>
    `:o}_renderContent(){return this._loading?t`<div class="placeholder">Loading templates…</div>`:this._templates.length===0?t`
        <div class="placeholder">
          No schedule templates saved yet. Save one from an existing
          schedule's editor ("Save as template").
        </div>
      `:t`
      <ul class="templates">
        ${this._templates.map(C=>t`
            <li class="template">
              <div class="template-info">
                <span class="template-name">${C.name}</span>
                <span class="template-meta">
                  ${V1[C.device_type]} ·
                  ${C.rules.length}
                  ${C.rules.length===1?"rule":"rules"}
                </span>
              </div>
              <div class="row-actions">
                <button type="button" class="btn" @click=${()=>this._useTemplate(C)}>
                  Use
                </button>
                <ha-icon-button
                  .path=${E}
                  label="Delete template"
                  @click=${()=>this._deleteTemplateRow(C)}
                ></ha-icon-button>
              </div>
            </li>
          `)}
      </ul>
    `}};w.styles=v`
    .form {
      display: flex;
      flex-direction: column;
      gap: 12px;
      min-width: 320px;
      max-width: 420px;
    }
    .dialog-title {
      font-size: 1.25rem;
      font-weight: 500;
      color: var(--primary-text-color);
    }
    .error {
      color: var(--error-color);
    }
    .placeholder {
      padding: 16px 0;
      color: var(--secondary-text-color);
      text-align: center;
    }
    ul.templates {
      list-style: none;
      margin: 0;
      padding: 0;
    }
    .template {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 8px 0;
      border-bottom: 1px solid var(--divider-color);
    }
    .template:last-child {
      border-bottom: none;
    }
    .template-info {
      display: flex;
      flex-direction: column;
      flex: 1;
      min-width: 0;
    }
    .template-name {
      font-weight: 500;
    }
    .template-meta {
      font-size: 0.85em;
      color: var(--secondary-text-color);
    }
    .row-actions {
      display: flex;
      align-items: center;
    }
    .dialog-actions {
      display: flex;
      justify-content: flex-end;
      gap: 8px;
      padding-top: 8px;
      border-top: 1px solid var(--divider-color);
    }
    .btn {
      font: inherit;
      font-weight: 500;
      font-size: 14px;
      padding: 8px 16px;
      border-radius: 6px;
      border: 1px solid var(--divider-color);
      background: var(--card-background-color);
      color: var(--primary-text-color);
      cursor: pointer;
    }
    .btn:hover {
      background: var(--secondary-background-color, rgba(0, 0, 0, 0.06));
    }
  `,r([n({attribute:!1})],w.prototype,"hass",2),r([a()],w.prototype,"_open",2),r([a()],w.prototype,"_templates",2),r([a()],w.prototype,"_loading",2),r([a()],w.prototype,"_error",2),w=r([x("scheduler-plus-apply-template-dialog")],w);var k=class extends m{constructor(){super(...arguments);this.value=[];this.domains=[];this._search="";this._pending=new Set;this._addSelected=()=>{this._pending.size!==0&&(this._fireChange([...this.value,...this._pending]),this._pending=new Set,this._search="")}}_entityName(C){let H=this.hass.states[C]?.attributes.friendly_name;return typeof H=="string"?H:C}get _candidates(){let C=this._search.trim().toLowerCase();return Object.keys(this.hass.states).filter(H=>this.domains.some(M=>H.startsWith(`${M}.`))).filter(H=>!this.value.includes(H)).filter(H=>!this.includeEntities||this.includeEntities.includes(H)).filter(H=>!C||H.toLowerCase().includes(C)||this._entityName(H).toLowerCase().includes(C)).sort((H,M)=>this._entityName(H).localeCompare(this._entityName(M)))}_fireChange(C){this.value=C,this.dispatchEvent(new CustomEvent("value-changed",{detail:{value:C}}))}_removeEntity(C){this._fireChange(this.value.filter(H=>H!==C))}_toggleCandidate(C){let H=new Set(this._pending);H.has(C)?H.delete(C):H.add(C),this._pending=H}render(){let C=this._pending.size>0?`Add ${this._pending.size} device${this._pending.size===1?"":"s"}`:"Add selected";return t`
      ${this.value.length>0?t`
            <ul class="selected">
              ${this.value.map(H=>t`
                  <li class="chip">
                    <span>${this._entityName(H)}</span>
                    <button
                      type="button"
                      class="chip-remove"
                      aria-label="Remove ${this._entityName(H)}"
                      @click=${()=>this._removeEntity(H)}
                    >
                      ×
                    </button>
                  </li>
                `)}
            </ul>
          `:o}

      <input
        type="text"
        class="native-input"
        placeholder="Search devices…"
        .value=${this._search}
        @input=${H=>{this._search=H.target.value}}
      />
      <div class="candidates">
        ${this._candidates.length===0?t`<div class="empty">No matching devices.</div>`:this._candidates.map(H=>t`
                <label class="candidate">
                  <input
                    type="checkbox"
                    .checked=${this._pending.has(H)}
                    @change=${()=>this._toggleCandidate(H)}
                  />
                  <span>${this._entityName(H)}</span>
                </label>
              `)}
      </div>
      <button
        type="button"
        class="btn btn-primary"
        ?disabled=${this._pending.size===0}
        @click=${this._addSelected}
      >
        ${C}
      </button>
    `}};k.styles=v`
    :host {
      display: flex;
      flex-direction: column;
      gap: 8px;
    }
    /* Wrapping chips instead of one full-width row per entity - a schedule
       with dozens of entities (a whole building's worth of rooms) used to
       mean dozens of rows and a lot of scrolling just to see them all. */
    ul.selected {
      list-style: none;
      margin: 0;
      padding: 0;
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      max-height: 220px;
      overflow-y: auto;
    }
    .chip {
      display: inline-flex;
      align-items: center;
      gap: 2px;
      padding: 4px 4px 4px 10px;
      border: 1px solid var(--divider-color);
      border-radius: 14px;
      background: var(--card-background-color);
      font-size: 0.85em;
      color: var(--primary-text-color);
      max-width: 100%;
    }
    .chip span {
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }
    .chip-remove {
      flex: none;
      font: inherit;
      font-size: 1rem;
      line-height: 1;
      width: 20px;
      height: 20px;
      border: none;
      border-radius: 50%;
      background: transparent;
      color: var(--secondary-text-color);
      cursor: pointer;
    }
    .chip-remove:hover {
      background: var(--secondary-background-color, rgba(0, 0, 0, 0.08));
      color: var(--primary-text-color);
    }
    .native-input {
      font: inherit;
      color: var(--primary-text-color);
      background: var(--card-background-color);
      border: 1px solid var(--divider-color);
      border-radius: 4px;
      padding: 8px;
    }
    .candidates {
      display: flex;
      flex-direction: column;
      max-height: 180px;
      overflow-y: auto;
      border: 1px solid var(--divider-color);
      border-radius: 6px;
    }
    .candidate {
      display: flex;
      align-items: center;
      gap: 8px;
      padding: 6px 10px;
      font-size: 0.9em;
      color: var(--primary-text-color);
      cursor: pointer;
    }
    .candidate:hover {
      background: var(--secondary-background-color, rgba(0, 0, 0, 0.04));
    }
    .candidate input {
      flex: none;
    }
    .empty {
      padding: 10px;
      font-size: 0.85em;
      color: var(--secondary-text-color);
      text-align: center;
    }
    .btn {
      font: inherit;
      font-weight: 500;
      font-size: 14px;
      padding: 8px 16px;
      border-radius: 6px;
      border: 1px solid var(--divider-color);
      background: var(--card-background-color);
      color: var(--primary-text-color);
      cursor: pointer;
      align-self: flex-start;
    }
    .btn:hover {
      background: var(--secondary-background-color, rgba(0, 0, 0, 0.06));
    }
    .btn:disabled {
      opacity: 0.5;
      cursor: default;
    }
    .btn-primary {
      background: var(--primary-color);
      border-color: var(--primary-color);
      color: var(--text-primary-color, #fff);
    }
    .btn-primary:hover {
      filter: brightness(0.95);
    }
    .btn-primary:disabled {
      background: var(--card-background-color);
      color: var(--primary-text-color);
    }
  `,r([n({attribute:!1})],k.prototype,"hass",2),r([n({attribute:!1})],k.prototype,"value",2),r([n({attribute:!1})],k.prototype,"domains",2),r([n({attribute:!1})],k.prototype,"includeEntities",2),r([a()],k.prototype,"_search",2),r([a()],k.prototype,"_pending",2),k=r([x("scheduler-plus-entity-multi-picker")],k);var j=class extends m{constructor(){super(...arguments);this._handleTitleChange=C=>{let H=C.target.value;this._fireConfigChanged({...this._config,title:H||void 0})}}setConfig(C){this._config=C}_fireConfigChanged(C){this._config=C,this.dispatchEvent(new CustomEvent("config-changed",{detail:{config:C}}))}render(){return this._config?t`
      <div class="editor">
        <label class="field-label" for="card-title">Title</label>
        <input
          id="card-title"
          type="text"
          class="native-input"
          .value=${this._config.title??""}
          @input=${this._handleTitleChange}
        />

        <label class="field-label">Devices to show</label>
        <span class="hint">
          Leave empty to show every schedule. Otherwise, only schedules
          targeting at least one of these devices appear in this card -
          useful for putting a device-specific card on a room's own
          dashboard page.
        </span>
        <scheduler-plus-entity-multi-picker
          .hass=${this.hass}
          .value=${this._config.entities??[]}
          .domains=${L1}
          @value-changed=${C=>{this._fireConfigChanged({...this._config,entities:C.detail.value})}}
        ></scheduler-plus-entity-multi-picker>
      </div>
    `:t``}};j.styles=v`
    .editor {
      display: flex;
      flex-direction: column;
      gap: 8px;
      padding: 8px 0;
    }
    .field-label {
      font-size: 0.85em;
      color: var(--secondary-text-color);
    }
    .hint {
      font-size: 0.85em;
      color: var(--secondary-text-color);
    }
    .native-input {
      font: inherit;
      color: var(--primary-text-color);
      background: var(--card-background-color);
      border: 1px solid var(--divider-color);
      border-radius: 4px;
      padding: 8px;
    }
  `,r([n({attribute:!1})],j.prototype,"hass",2),r([a()],j.prototype,"_config",2),j=r([x("scheduler-plus-card-editor")],j);function e1(L,V){if(L==="light"){let C=[];return typeof V.brightness=="number"&&C.push(`Brightness ${Math.round(V.brightness/255*100)}%`),typeof V.transition=="number"&&V.transition>0&&C.push(`fade ${V.transition}s`),C.length>0?C.join(" \xB7 "):void 0}if(L==="climate"){let C=[];if(typeof V.hvac_mode=="string"){let H=M1;C.push(H[V.hvac_mode]??V.hvac_mode)}return typeof V.target_temperature=="number"&&C.push(`${V.target_temperature}\xB0`),C.length>0?C.join(" \xB7 "):void 0}}function $2(L){let V=L.getFullYear(),C=String(L.getMonth()+1).padStart(2,"0"),H=String(L.getDate()).padStart(2,"0");return`${V}-${C}-${H}`}function B1(){return $2(new Date)}function N(L){return new Date(L).toLocaleTimeString(void 0,{hour:"numeric",minute:"2-digit"})}function P1(L,V){let C=new Date(L),H=$2(C);return H>V?100:H<V?0:(C.getHours()*60+C.getMinutes())/(24*60)*100}function N2(L,V){let C=`${L.schedule_name} \xB7 ${L.rule_name}`;if(L.on_at!==null&&L.off_at!==null){let H=P1(L.on_at,V),M=P1(L.off_at,V);return{leftPct:H,widthPct:Math.max(M-H,1),title:`${C} (${N(L.on_at)} \u2192 ${N(L.off_at)})`}}return L.on_at!==null?{leftPct:P1(L.on_at,V),widthPct:1,title:`${C} (on at ${N(L.on_at)})`}:{leftPct:P1(L.off_at,V),widthPct:1,title:`${C} (off at ${N(L.off_at)})`}}function n5(L){return L.events.map(V=>N2(V,L.date))}function l5(L,V,C,H){return L<H&&C<V}function m5(L,V){let{on_at:C,off_at:H,schedule_id:M}=L;return C===null||H===null?!1:V.some(e=>e.schedule_id===M||e.on_at===null||e.off_at===null?!1:l5(C,H,e.on_at,e.off_at))}function v5(L,V){let C=new Map;for(let H of L){let M=C.get(H.schedule_id);M?M.push(H):C.set(H.schedule_id,[H])}return[...C.values()].map(H=>{let[{schedule_id:M,schedule_name:e}]=H,i=H.map(d=>({...N2(d,V),conflict:m5(d,L)}));return{scheduleId:M,scheduleName:e,segments:i}}).sort((H,M)=>H.scheduleName.localeCompare(M.scheduleName))}function x5(L,V=4){let C=L.join(", ");return L.length<=V?{visible:C,full:C,overflow:0}:{visible:L.slice(0,V).join(", "),full:C,overflow:L.length-V}}var I2=["devices","climate"],W2={devices:"Lights & Switches",climate:"Climate"};function E2(L){return L==="climate"?"climate":"devices"}var s5=["all",...I2],Z5={all:"All",...W2},h=class extends m{constructor(){super(...arguments);this._open=!1;this._viewMode="day";this._date=B1();this._reportFilter="all";this._events=[];this._weekDays=[];this._loading=!1;this._closeDialog=()=>{this._open=!1};this._handleViewModeChange=C=>{this._viewMode=C,this._load()};this._handleDateChange=C=>{this._date=C.target.value,this._load()};this._handleReportFilterChange=C=>{this._reportFilter=C.target.value,this._load()}}showDialog(){this._viewMode="day",this._date=B1(),this._reportFilter="all",this._open=!0,this._load()}_entityName(C){let H=this.hass.states[C]?.attributes.friendly_name;return typeof H=="string"?H:C}_matchesFilters(C){if(this._reportFilter!=="all"&&E2(C.device_type)!==this._reportFilter)return!1;let H=this.entityFilter;return!(H&&H.length>0&&!C.entities.some(M=>H.includes(M)))}async _load(){this._loading=!0,this._error=void 0;try{if(this._viewMode==="day"){let C=await k2(this.hass,this._date);this._events=C.filter(H=>this._matchesFilters(H))}else{let C=await _2(this.hass,this._date);this._weekDays=C.map(H=>({date:H.date,events:H.events.filter(M=>this._matchesFilters(M))}))}}catch(C){this._error=C instanceof Error?C.message:String(C)}finally{this._loading=!1}}render(){return this._open?t`
      <ha-dialog open @closed=${this._closeDialog}>
        <div class="form">
          <div class="dialog-title">Day view</div>

          <div class="view-toggle">
            <button
              type="button"
              class="day-chip ${this._viewMode==="day"?"active":""}"
              @click=${()=>this._handleViewModeChange("day")}
            >
              Day
            </button>
            <button
              type="button"
              class="day-chip ${this._viewMode==="week"?"active":""}"
              @click=${()=>this._handleViewModeChange("week")}
            >
              Week
            </button>
          </div>

          <div class="controls">
            <div class="control">
              <label class="field-label" for="day-view-date">
                ${this._viewMode==="week"?"Week starting":"Date"}
              </label>
              <input
                id="day-view-date"
                type="date"
                class="native-input"
                .value=${this._date}
                @change=${this._handleDateChange}
              />
            </div>
            <div class="control">
              <label class="field-label" for="day-view-device-type">Device type</label>
              <select
                id="day-view-device-type"
                class="native-select"
                .value=${this._reportFilter}
                @change=${this._handleReportFilterChange}
              >
                ${s5.map(C=>t`<option value=${C}>${Z5[C]}</option>`)}
              </select>
            </div>
          </div>

          <div class="content">
            ${this._viewMode==="day"?this._renderContent():this._renderWeekContent()}
          </div>

          <div class="dialog-actions">
            <button type="button" class="btn" @click=${this._closeDialog}>Close</button>
          </div>
        </div>
      </ha-dialog>
    `:o}_renderContent(){if(this._loading)return t`<div class="placeholder">Loading…</div>`;if(this._error)return t`<div class="placeholder error">${this._error}</div>`;if(this._events.length===0)return t`<div class="placeholder">No activity scheduled for this day.</div>`;let C=I2.map(H=>({group:H,events:this._events.filter(M=>E2(M.device_type)===H).sort((M,e)=>(M.on_at??M.off_at??"").localeCompare(e.on_at??e.off_at??""))})).filter(H=>H.events.length>0);return t`
      ${this._renderDayTimeline()}
      ${C.map(H=>t`
          <div class="group">
            <h3 class="group-title">${W2[H.group]}</h3>
            <ul class="events">
              ${H.events.map(M=>this._renderEvent(M))}
            </ul>
          </div>
        `)}
    `}_renderDayTimeline(){let C=v5(this._events,this._date),H=this._date===B1(),M=new Date,e=H?(M.getHours()*60+M.getMinutes())/(24*60)*100:null;return t`
      <div class="swimlanes">
        ${C.map(i=>t`
            <div class="swimlane">
              <span class="swimlane-label" title=${i.scheduleName}>${i.scheduleName}</span>
              <div class="day-timeline" title="12 AM to 12 AM">
                <span class="day-timeline-tick" style="left: 25%"></span>
                <span class="day-timeline-tick" style="left: 50%"></span>
                <span class="day-timeline-tick" style="left: 75%"></span>
                ${i.segments.map(d=>t`
                    <span
                      class="day-timeline-segment ${d.conflict?"conflict":""}"
                      style="left: ${d.leftPct}%; width: ${d.widthPct}%"
                      title="${d.conflict?"\u26A0 Overlaps another schedule - ":""}${d.title}"
                    ></span>
                  `)}
                ${e!==null?t`<span class="day-timeline-now" style="left: ${e}%" title="Now"></span>`:o}
              </div>
            </div>
          `)}
      </div>
    `}_renderWeekContent(){return this._loading?t`<div class="placeholder">Loading…</div>`:this._error?t`<div class="placeholder error">${this._error}</div>`:this._weekDays.every(C=>C.events.length===0)?t`<div class="placeholder">No activity scheduled this week.</div>`:t`
      <div class="week-list">${this._weekDays.map(C=>this._renderWeekDay(C))}</div>
    `}_renderWeekDay(C){let H=new Date(`${C.date}T00:00:00`).toLocaleDateString(void 0,{weekday:"long",month:"short",day:"numeric"}),M=C.date===B1(),e=[...C.events].sort((l,Z)=>(l.on_at??l.off_at??"").localeCompare(Z.on_at??Z.off_at??"")),i=n5(C),d=new Date,A=M?(d.getHours()*60+d.getMinutes())/(24*60)*100:null;return t`
      <div class="week-day">
        <div class="week-day-header">
          <span class=${M?"today":""}>${H}</span>
          ${M?t`<span class="today-badge">Today</span>`:o}
        </div>
        <div class="day-timeline" title="12 AM to 12 AM">
          <span class="day-timeline-tick" style="left: 25%"></span>
          <span class="day-timeline-tick" style="left: 50%"></span>
          <span class="day-timeline-tick" style="left: 75%"></span>
          ${i.map(l=>t`
              <span
                class="day-timeline-segment"
                style="left: ${l.leftPct}%; width: ${l.widthPct}%"
                title=${l.title}
              ></span>
            `)}
          ${A!==null?t`<span class="day-timeline-now" style="left: ${A}%" title="Now"></span>`:o}
        </div>
        ${e.length===0?t`<div class="placeholder small">Nothing scheduled</div>`:t`<ul class="events">${e.map(l=>this._renderEvent(l))}</ul>`}
      </div>
    `}_renderEvent(C){let H=C.on_at!==null&&C.off_at!==null&&C.off_at.slice(0,10)!==C.on_at.slice(0,10),M=e1(C.device_type,C.action),e=x5(C.entities.map(i=>this._entityName(i)));return t`
      <li class="event">
        <div class="event-top">
          <span class="event-time">
            ${C.on_at!==null&&C.off_at!==null?t`${N(C.on_at)} → ${N(C.off_at)}`:C.on_at!==null?t`On at ${N(C.on_at)}`:t`Off at ${N(C.off_at)}`}
          </span>
          ${H?t`<span class="hint">next day</span>`:o}
        </div>
        <span class="event-name">${C.schedule_name} · ${C.rule_name}</span>
        ${M?t`<span class="event-action">${M}</span>`:o}
        <span class="event-entities" title=${e.full}>
          ${e.visible}${e.overflow>0?t` <span class="event-entities-more">+${e.overflow} more</span>`:o}
        </span>
      </li>
    `}};h.styles=v`
    .form {
      display: flex;
      flex-direction: column;
      gap: 16px;
      min-width: 320px;
      max-width: min(92vw, 520px);
    }
    .dialog-title {
      font-size: 1.25rem;
      font-weight: 500;
      color: var(--primary-text-color);
    }
    .view-toggle {
      display: flex;
      gap: 4px;
    }
    .day-chip {
      font: inherit;
      color: var(--primary-text-color);
      background: var(--card-background-color);
      border: 1px solid var(--divider-color);
      border-radius: 16px;
      padding: 6px 12px;
      cursor: pointer;
    }
    .day-chip.active {
      color: var(--text-primary-color, #fff);
      background: var(--primary-color);
      border-color: var(--primary-color);
    }
    .week-list {
      display: flex;
      flex-direction: column;
      gap: 14px;
    }
    .week-day {
      border: 1px solid var(--divider-color);
      border-radius: 10px;
      padding: 10px 12px;
    }
    .week-day-header {
      display: flex;
      align-items: center;
      gap: 8px;
      margin-bottom: 8px;
      font-size: 0.9em;
      font-weight: 600;
      color: var(--primary-text-color);
    }
    .week-day-header .today {
      color: var(--primary-color);
    }
    .today-badge {
      font-size: 0.68em;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.03em;
      color: var(--text-primary-color, #fff);
      background: var(--primary-color);
      padding: 2px 8px;
      border-radius: 10px;
    }
    .placeholder.small {
      padding: 0;
      font-size: 0.85em;
      text-align: left;
    }
    .day-timeline {
      position: relative;
      height: 8px;
      margin-bottom: 10px;
      border-radius: 4px;
      background: var(--secondary-background-color, rgba(0, 0, 0, 0.06));
      overflow: hidden;
    }
    .day-timeline-tick {
      position: absolute;
      top: 0;
      bottom: 0;
      width: 1px;
      background: var(--card-background-color);
      opacity: 0.6;
    }
    .day-timeline-segment {
      position: absolute;
      top: 0;
      bottom: 0;
      min-width: 3px;
      border-radius: 3px;
      background: var(--primary-color);
      opacity: 0.8;
    }
    .day-timeline-now {
      position: absolute;
      top: -2px;
      bottom: -2px;
      width: 2px;
      background: var(--error-color, #db4437);
      border-radius: 1px;
    }
    .day-timeline-segment.conflict {
      background: var(--warning-color, #ffa600);
      opacity: 1;
    }
    .swimlanes {
      display: flex;
      flex-direction: column;
      gap: 8px;
      margin-bottom: 18px;
    }
    .swimlane {
      display: grid;
      grid-template-columns: minmax(0, 100px) 1fr;
      align-items: center;
      gap: 8px;
    }
    .swimlane-label {
      font-size: 0.78em;
      color: var(--secondary-text-color);
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }
    .swimlane .day-timeline {
      margin-bottom: 0;
    }
    .controls {
      display: flex;
      gap: 12px;
    }
    .control {
      flex: 1;
      display: flex;
      flex-direction: column;
      gap: 4px;
    }
    .field-label {
      font-size: 0.85em;
      color: var(--secondary-text-color);
    }
    .native-select,
    .native-input {
      font: inherit;
      color: var(--primary-text-color);
      background: var(--card-background-color);
      border: 1px solid var(--divider-color);
      border-radius: 4px;
      padding: 8px;
    }
    .content {
      min-height: 80px;
    }
    .placeholder {
      padding: 16px 0;
      color: var(--secondary-text-color);
      text-align: center;
    }
    .placeholder.error {
      color: var(--error-color);
    }
    .group {
      margin-bottom: 18px;
    }
    .group:last-child {
      margin-bottom: 0;
    }
    .group-title {
      margin: 0 0 8px;
      font-size: 0.8em;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.03em;
      color: var(--secondary-text-color);
    }
    ul.events {
      list-style: none;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      gap: 6px;
    }
    .event {
      display: flex;
      flex-direction: column;
      gap: 3px;
      padding: 10px 12px;
      border-radius: 8px;
      background: var(--secondary-background-color, rgba(0, 0, 0, 0.035));
    }
    .event-top {
      display: flex;
      align-items: baseline;
      gap: 8px;
    }
    .event-time {
      font-weight: 600;
      font-size: 0.9em;
      color: var(--primary-color);
    }
    .event-name {
      font-size: 0.9em;
      font-weight: 500;
      color: var(--primary-text-color);
    }
    .event-action {
      font-size: 0.85em;
      color: var(--primary-color);
    }
    .event-entities {
      font-size: 0.8em;
      color: var(--secondary-text-color);
    }
    .event-entities-more {
      font-style: italic;
    }
    .hint {
      font-size: 0.85em;
      color: var(--secondary-text-color);
    }
    .dialog-actions {
      display: flex;
      justify-content: flex-end;
      gap: 8px;
      padding-top: 8px;
      border-top: 1px solid var(--divider-color);
    }
    .btn {
      font: inherit;
      font-weight: 500;
      font-size: 14px;
      padding: 8px 16px;
      border-radius: 6px;
      border: 1px solid var(--divider-color);
      background: var(--card-background-color);
      color: var(--primary-text-color);
      cursor: pointer;
    }
    .btn:hover {
      background: var(--secondary-background-color, rgba(0, 0, 0, 0.06));
    }
  `,r([n({attribute:!1})],h.prototype,"hass",2),r([n({attribute:!1})],h.prototype,"entityFilter",2),r([a()],h.prototype,"_open",2),r([a()],h.prototype,"_viewMode",2),r([a()],h.prototype,"_date",2),r([a()],h.prototype,"_reportFilter",2),r([a()],h.prototype,"_events",2),r([a()],h.prototype,"_weekDays",2),r([a()],h.prototype,"_loading",2),r([a()],h.prototype,"_error",2),h=r([x("scheduler-plus-day-view")],h);var _=class extends m{constructor(){super(...arguments);this._open=!1;this._until="";this._saving=!1;this._closeDialog=()=>{this._open=!1};this._save=async()=>{if(this._schedule){if(!this._until){this._error="Pick a date to pause through.";return}this._saving=!0,this._error=void 0;try{await D(this.hass,this._schedule.id,{...Q(this._schedule),override_until:this._until}),this._open=!1,this.dispatchEvent(new CustomEvent("schedule-plus-saved"))}catch(C){this._error=C instanceof Error?C.message:String(C)}finally{this._saving=!1}}}}showDialog(C){this._schedule=C,this._until=C.override_until??"",this._error=void 0,this._open=!0}render(){return!this._open||!this._schedule?o:t`
      <ha-dialog open @closed=${this._closeDialog}>
        <div class="form">
          <div class="dialog-title">Pause "${this._schedule.name}"</div>
          <span class="hint">
            Suppresses this schedule entirely through the date below, then
            resumes automatically the next day - nothing to remember to turn
            back on.
          </span>
          ${this._error?t`<div class="error">${this._error}</div>`:o}

          <label class="field-label" for="override-until">Paused through</label>
          <input
            id="override-until"
            type="date"
            class="native-input"
            .value=${this._until}
            @input=${C=>{this._until=C.target.value}}
          />

          <div class="dialog-actions">
            <button type="button" class="btn" @click=${this._closeDialog}>Cancel</button>
            <button
              type="button"
              class="btn btn-primary"
              ?disabled=${this._saving}
              @click=${this._save}
            >
              Pause
            </button>
          </div>
        </div>
      </ha-dialog>
    `}};_.styles=v`
    .form {
      display: flex;
      flex-direction: column;
      gap: 12px;
      min-width: 320px;
      max-width: 420px;
    }
    .dialog-title {
      font-size: 1.25rem;
      font-weight: 500;
      color: var(--primary-text-color);
    }
    .hint {
      font-size: 0.85em;
      color: var(--secondary-text-color);
    }
    .error {
      color: var(--error-color);
    }
    .field-label {
      font-size: 0.85em;
      color: var(--secondary-text-color);
    }
    .native-input {
      font: inherit;
      color: var(--primary-text-color);
      background: var(--card-background-color);
      border: 1px solid var(--divider-color);
      border-radius: 4px;
      padding: 8px;
    }
    .dialog-actions {
      display: flex;
      justify-content: flex-end;
      gap: 8px;
      padding-top: 8px;
      border-top: 1px solid var(--divider-color);
    }
    .btn {
      font: inherit;
      font-weight: 500;
      font-size: 14px;
      padding: 8px 16px;
      border-radius: 6px;
      border: 1px solid var(--divider-color);
      background: var(--card-background-color);
      color: var(--primary-text-color);
      cursor: pointer;
    }
    .btn:hover {
      background: var(--secondary-background-color, rgba(0, 0, 0, 0.06));
    }
    .btn:disabled {
      opacity: 0.5;
      cursor: default;
    }
    .btn-primary {
      background: var(--primary-color);
      border-color: var(--primary-color);
      color: var(--text-primary-color, #fff);
    }
    .btn-primary:hover {
      filter: brightness(0.95);
    }
  `,r([n({attribute:!1})],_.prototype,"hass",2),r([a()],_.prototype,"_open",2),r([a()],_.prototype,"_schedule",2),r([a()],_.prototype,"_until",2),r([a()],_.prototype,"_saving",2),r([a()],_.prototype,"_error",2),_=r([x("scheduler-plus-override-dialog")],_);var g=class extends m{constructor(){super(...arguments);this._open=!1;this._weekdayDays=[];this._weekendDays=[];this._workingHoursStart="09:00";this._workingHoursEnd="17:00";this._loading=!1;this._saving=!1;this._closeDialog=()=>{this._open=!1};this._toggleWeekdayDay=C=>{this._weekdayDays=this._weekdayDays.includes(C)?this._weekdayDays.filter(H=>H!==C):[...this._weekdayDays,C],this._weekendDays=this._weekendDays.filter(H=>H!==C)};this._toggleWeekendDay=C=>{this._weekendDays=this._weekendDays.includes(C)?this._weekendDays.filter(H=>H!==C):[...this._weekendDays,C],this._weekdayDays=this._weekdayDays.filter(H=>H!==C)};this._save=async()=>{if(this._weekdayDays.length===0){this._error="At least one weekday day is required.";return}if(this._weekendDays.length===0){this._error="At least one weekend day is required.";return}this._saving=!0,this._error=void 0;try{let C={weekday_days:this._weekdayDays,weekend_days:this._weekendDays,working_hours_start:this._workingHoursStart,working_hours_end:this._workingHoursEnd};await b2(this.hass,C),this._open=!1}catch(C){this._error=C instanceof Error?C.message:String(C)}finally{this._saving=!1}}}showDialog(){this._open=!0,this._load()}async _load(){this._loading=!0,this._error=void 0;try{let C=await f1(this.hass);this._weekdayDays=[...C.weekday_days],this._weekendDays=[...C.weekend_days],this._workingHoursStart=C.working_hours_start.slice(0,5),this._workingHoursEnd=C.working_hours_end.slice(0,5)}catch(C){this._error=C instanceof Error?C.message:String(C)}finally{this._loading=!1}}render(){return this._open?t`
      <ha-dialog open @closed=${this._closeDialog}>
        <div class="form">
          <div class="dialog-title">My preferences</div>
          <span class="hint">
            Your own weekday/weekend/working-hours split, used by the rule
            editor's quick-fill presets. Only affects your account - not
            shared with other users.
          </span>
          ${this._error?t`<div class="error">${this._error}</div>`:o}

          ${this._loading?t`<div class="placeholder">Loading…</div>`:t`
                <label class="field-label">Weekday days</label>
                <div class="days">
                  ${b.map(C=>t`
                      <button
                        type="button"
                        class="day-chip ${this._weekdayDays.includes(C)?"active":""}"
                        @click=${()=>this._toggleWeekdayDay(C)}
                      >
                        ${q[C].slice(0,3)}
                      </button>
                    `)}
                </div>

                <label class="field-label">Weekend days</label>
                <div class="days">
                  ${b.map(C=>t`
                      <button
                        type="button"
                        class="day-chip ${this._weekendDays.includes(C)?"active":""}"
                        @click=${()=>this._toggleWeekendDay(C)}
                      >
                        ${q[C].slice(0,3)}
                      </button>
                    `)}
                </div>

                <div class="controls">
                  <div class="control">
                    <label class="field-label" for="working-hours-start">
                      Working hours start
                    </label>
                    <input
                      id="working-hours-start"
                      type="time"
                      class="native-input"
                      .value=${this._workingHoursStart}
                      @input=${C=>{this._workingHoursStart=C.target.value}}
                    />
                  </div>
                  <div class="control">
                    <label class="field-label" for="working-hours-end">
                      Working hours end
                    </label>
                    <input
                      id="working-hours-end"
                      type="time"
                      class="native-input"
                      .value=${this._workingHoursEnd}
                      @input=${C=>{this._workingHoursEnd=C.target.value}}
                    />
                  </div>
                </div>
              `}

          <div class="dialog-actions">
            <button type="button" class="btn" @click=${this._closeDialog}>Cancel</button>
            <button
              type="button"
              class="btn btn-primary"
              ?disabled=${this._loading||this._saving}
              @click=${this._save}
            >
              Save
            </button>
          </div>
        </div>
      </ha-dialog>
    `:o}};g.styles=v`
    .form {
      display: flex;
      flex-direction: column;
      gap: 12px;
      min-width: 320px;
      max-width: 420px;
    }
    .dialog-title {
      font-size: 1.25rem;
      font-weight: 500;
      color: var(--primary-text-color);
    }
    .hint {
      font-size: 0.85em;
      color: var(--secondary-text-color);
    }
    .error {
      color: var(--error-color);
    }
    .placeholder {
      padding: 16px 0;
      color: var(--secondary-text-color);
      text-align: center;
    }
    .field-label {
      font-size: 0.85em;
      color: var(--secondary-text-color);
    }
    .days {
      display: flex;
      flex-wrap: wrap;
      gap: 4px;
    }
    .day-chip {
      font: inherit;
      color: var(--primary-text-color);
      background: var(--card-background-color);
      border: 1px solid var(--divider-color);
      border-radius: 16px;
      padding: 6px 12px;
      cursor: pointer;
    }
    .day-chip.active {
      color: var(--text-primary-color, #fff);
      background: var(--primary-color);
      border-color: var(--primary-color);
    }
    .controls {
      display: flex;
      gap: 12px;
    }
    .control {
      flex: 1;
      display: flex;
      flex-direction: column;
      gap: 4px;
    }
    .native-input {
      font: inherit;
      color: var(--primary-text-color);
      background: var(--card-background-color);
      border: 1px solid var(--divider-color);
      border-radius: 4px;
      padding: 8px;
    }
    .dialog-actions {
      display: flex;
      justify-content: flex-end;
      gap: 8px;
      padding-top: 8px;
      border-top: 1px solid var(--divider-color);
    }
    .btn {
      font: inherit;
      font-weight: 500;
      font-size: 14px;
      padding: 8px 16px;
      border-radius: 6px;
      border: 1px solid var(--divider-color);
      background: var(--card-background-color);
      color: var(--primary-text-color);
      cursor: pointer;
    }
    .btn:hover {
      background: var(--secondary-background-color, rgba(0, 0, 0, 0.06));
    }
    .btn:disabled {
      opacity: 0.5;
      cursor: default;
    }
    .btn-primary {
      background: var(--primary-color);
      border-color: var(--primary-color);
      color: var(--text-primary-color, #fff);
    }
    .btn-primary:hover {
      filter: brightness(0.95);
    }
  `,r([n({attribute:!1})],g.prototype,"hass",2),r([a()],g.prototype,"_open",2),r([a()],g.prototype,"_weekdayDays",2),r([a()],g.prototype,"_weekendDays",2),r([a()],g.prototype,"_workingHoursStart",2),r([a()],g.prototype,"_workingHoursEnd",2),r([a()],g.prototype,"_loading",2),r([a()],g.prototype,"_saving",2),r([a()],g.prototype,"_error",2),g=r([x("scheduler-plus-preferences")],g);function U2(L){return new Date(L).toLocaleTimeString(void 0,{hour:"numeric",minute:"2-digit"})}function c5(L){return new Date(`${L}T00:00:00`).toLocaleDateString(void 0,{month:"short",day:"numeric"})}function R1(L){return`Overlaps "${L.conflicting_schedule_name}" -> "${L.conflicting_rule_name}" on ${c5(L.date)}, ${U2(L.conflicting_on_at)} - ${U2(L.conflicting_off_at)}`}async function D1(L,V){let H=(await h1(L)).find(i=>i.id===V.conflicting_schedule_id);if(!H)throw new Error(`"${V.conflicting_schedule_name}" no longer exists.`);let M=!1,e=H.rules.map(i=>i.id!==V.conflicting_rule_id?i:(M=!0,i.date_mode==="include"?{...i,dates:i.dates.filter(d=>d!==V.date)}:{...i,date_mode:"exclude",dates:i.dates.includes(V.date)?i.dates:[...i.dates,V.date]}));if(!M)throw new Error(`"${V.conflicting_rule_name}" no longer exists.`);await D(L,H.id,{...Q(H),rules:e})}function G2(){let L=new Date;return`${L.getFullYear()}-${String(L.getMonth()+1).padStart(2,"0")}-${String(L.getDate()).padStart(2,"0")}`}var u=class extends m{constructor(){super(...arguments);this._open=!1;this._entities=[];this._name="";this._date=G2();this._onTime="18:00";this._offTime="22:00";this._saving=!1;this._checkingConflicts=!1;this._conflicts=[];this._closeDialog=()=>{this._open=!1};this._save=async()=>{if(this._entities.length===0){this._error="At least one entity is required.";return}let C=this._buildInput();this._checkingConflicts=!0,this._error=void 0;try{let H=await b1(this.hass,null,C);H.length===0?(this._conflicts=[],await this._persist(C)):this._conflicts=H}catch(H){this._error=H instanceof Error?H.message:String(H)}finally{this._checkingConflicts=!1}};this._createAnyway=()=>{this._persist(this._buildInput())};this._excludeConflict=async C=>{try{await D1(this.hass,C),this._conflicts=this._conflicts.filter(H=>!(H.conflicting_rule_id===C.conflicting_rule_id&&H.date===C.date)),this._conflicts.length===0&&await this._persist(this._buildInput())}catch(H){window.alert(H instanceof Error?H.message:String(H))}}}showDialog(){this._entities=[],this._date=G2(),this._name="",this._onTime="18:00",this._offTime="22:00",this._conflicts=[],this._error=void 0,this._open=!0}_buildInput(){let C={name:"Quick event",enabled:!0,days:[...b],date_mode:"include",dates:[this._date],date_ranges:[],day_conditions:[],on_time:{provider:"fixed",params:{time:this._onTime}},off_time:{provider:"fixed",params:{time:this._offTime}},on_enabled:!0,off_enabled:!0,allow_override:!0,override_grace_minutes:15,action:{},off_action:null};return{name:this._name.trim()||`Event \u2013 ${this._date}`,device_type:"light_switch",entities:this._entities,enabled:!0,rules:[C]}}async _persist(C){this._saving=!0,this._error=void 0;try{await g1(this.hass,C),this._open=!1,this.dispatchEvent(new CustomEvent("schedule-plus-saved"))}catch(H){this._error=H instanceof Error?H.message:String(H)}finally{this._saving=!1}}render(){return this._open?t`
      <ha-dialog open @closed=${this._closeDialog}>
        <div class="form">
          <div class="dialog-title">Quick event</div>
          <span class="hint">
            A one-off on/off for a single date - lights and switches only.
            For climate or a recurring schedule, use Add schedule instead.
          </span>
          ${this._error?t`<div class="error">${this._error}</div>`:o}

          <label class="field-label" for="event-name">Name</label>
          <input
            id="event-name"
            type="text"
            class="native-input"
            placeholder="Event – ${this._date}"
            .value=${this._name}
            @input=${C=>{this._name=C.target.value}}
          />

          <label class="field-label">Entities</label>
          <scheduler-plus-entity-multi-picker
            .hass=${this.hass}
            .value=${this._entities}
            .domains=${k1.light_switch}
            .includeEntities=${this.entityFilter}
            @value-changed=${C=>{this._entities=C.detail.value}}
          ></scheduler-plus-entity-multi-picker>

          <label class="field-label" for="event-date">Date</label>
          <input
            id="event-date"
            type="date"
            class="native-input"
            .value=${this._date}
            @input=${C=>{this._date=C.target.value}}
          />

          <div class="time-columns">
            <div class="time-field">
              <label class="field-label" for="event-on-time">On</label>
              <input
                id="event-on-time"
                type="time"
                class="native-input"
                .value=${this._onTime}
                @input=${C=>{this._onTime=C.target.value}}
              />
            </div>
            <div class="time-field">
              <label class="field-label" for="event-off-time">Off</label>
              <input
                id="event-off-time"
                type="time"
                class="native-input"
                .value=${this._offTime}
                @input=${C=>{this._offTime=C.target.value}}
              />
            </div>
          </div>

          ${this._conflicts.length>0?this._renderConflictPanel():o}

          <div class="dialog-actions">
            <button
              type="button"
              class="btn"
              ?disabled=${this._saving||this._checkingConflicts}
              @click=${this._closeDialog}
            >
              Cancel
            </button>
            <button
              type="button"
              class="btn btn-primary"
              ?disabled=${this._saving||this._checkingConflicts}
              @click=${this._save}
            >
              ${this._checkingConflicts?"Checking\u2026":"Create"}
            </button>
          </div>
        </div>
      </ha-dialog>
    `:o}_renderConflictPanel(){return t`
      <div class="conflict-panel">
        <span class="conflict-title">
          This overlaps ${this._conflicts.length===1?"another schedule":"other schedules"}
        </span>
        <ul class="conflicts">
          ${this._conflicts.map(C=>t`
              <li class="conflict-row">
                <div class="conflict-info">
                  <span>${R1(C)}</span>
                  <span class="hint">${C.entity_ids.join(", ")}</span>
                </div>
                ${C.fixable?t`
                      <button
                        type="button"
                        class="btn"
                        @click=${()=>this._excludeConflict(C)}
                      >
                        Exclude "${C.conflicting_schedule_name}" on ${C.date}
                      </button>
                    `:t`<span class="hint">Adjust manually - can't auto-fix this one.</span>`}
              </li>
            `)}
        </ul>
        <button type="button" class="btn" @click=${this._createAnyway}>Create anyway</button>
      </div>
    `}};u.styles=v`
    .form {
      display: flex;
      flex-direction: column;
      gap: 12px;
      min-width: 320px;
      max-width: 420px;
    }
    .dialog-title {
      font-size: 1.25rem;
      font-weight: 500;
      color: var(--primary-text-color);
    }
    .hint {
      font-size: 0.85em;
      color: var(--secondary-text-color);
    }
    .error {
      color: var(--error-color);
    }
    .field-label {
      font-size: 0.85em;
      color: var(--secondary-text-color);
    }
    .native-input {
      font: inherit;
      color: var(--primary-text-color);
      background: var(--card-background-color);
      border: 1px solid var(--divider-color);
      border-radius: 4px;
      padding: 8px;
    }
    .time-columns {
      display: flex;
      gap: 12px;
    }
    .time-field {
      flex: 1;
      display: flex;
      flex-direction: column;
      gap: 4px;
    }
    .conflict-panel {
      display: flex;
      flex-direction: column;
      gap: 8px;
      padding: 12px;
      border: 1px solid var(--warning-color, #ffa600);
      border-radius: 6px;
    }
    .conflict-title {
      font-weight: 500;
      color: var(--warning-color, #ffa600);
    }
    ul.conflicts {
      list-style: none;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      gap: 8px;
    }
    .conflict-row {
      display: flex;
      flex-direction: column;
      gap: 4px;
      padding-bottom: 8px;
      border-bottom: 1px solid var(--divider-color);
    }
    .conflict-row:last-child {
      border-bottom: none;
      padding-bottom: 0;
    }
    .conflict-info {
      display: flex;
      flex-direction: column;
      font-size: 0.9em;
    }
    .dialog-actions {
      display: flex;
      justify-content: flex-end;
      gap: 8px;
      padding-top: 8px;
      border-top: 1px solid var(--divider-color);
    }
    .btn {
      font: inherit;
      font-weight: 500;
      font-size: 14px;
      padding: 8px 16px;
      border-radius: 6px;
      border: 1px solid var(--divider-color);
      background: var(--card-background-color);
      color: var(--primary-text-color);
      cursor: pointer;
    }
    .btn:hover {
      background: var(--secondary-background-color, rgba(0, 0, 0, 0.06));
    }
    .btn:disabled {
      opacity: 0.5;
      cursor: default;
    }
    .btn-primary {
      background: var(--primary-color);
      border-color: var(--primary-color);
      color: var(--text-primary-color, #fff);
    }
    .btn-primary:hover {
      filter: brightness(0.95);
    }
  `,r([n({attribute:!1})],u.prototype,"hass",2),r([n({attribute:!1})],u.prototype,"entityFilter",2),r([a()],u.prototype,"_open",2),r([a()],u.prototype,"_entities",2),r([a()],u.prototype,"_name",2),r([a()],u.prototype,"_date",2),r([a()],u.prototype,"_onTime",2),r([a()],u.prototype,"_offTime",2),r([a()],u.prototype,"_saving",2),r([a()],u.prototype,"_checkingConflicts",2),r([a()],u.prototype,"_conflicts",2),r([a()],u.prototype,"_error",2),u=r([x("scheduler-plus-quick-event-dialog")],u);function X1(L,V,C){let H=new Date(L).getTime(),M=new Date(V).getTime(),e=new Date(C).getTime();if(!(e>M))return 0;let i=(H-M)/(e-M)*100;return Math.min(100,Math.max(0,i))}function z2(L,V,C){let H=new Date(V).getTime(),M=new Date(C).getTime(),e=Math.min(100,Math.max(0,L));return new Date(H+(M-H)*e/100).toISOString()}function Q2(L,V,C){let H=[];for(let M=0;M<L.length;M+=1){let e=L[M];if(!e||e.state!=="on")continue;let i=L[M+1],d=X1(e.at,V,C),A=i?X1(i.at,V,C):100;H.push({leftPct:d,widthPct:Math.max(A-d,.4),title:i?`On: ${r1(e.at)} \u2192 ${r1(i.at)}`:`On since ${r1(e.at)} (ongoing)`})}return H}function J1(L,V){return L.map(C=>({at:C.at,value:C.attributes[V]})).filter(C=>typeof C.value=="number")}function K2(L,V){let C=V.flatMap(d=>J1(L,d).map(A=>A.value));if(C.length===0)return null;let H=Math.min(...C),M=Math.max(...C),i=(M-H||Math.abs(M)||1)*.08;return{min:H-i,max:M+i}}function F1(L,V){let C=V.max-V.min||1;return 100-(L-V.min)/C*100}function C2(L,V,C,H,M){let e=J1(L,V);return e.length===0?null:{path:e.map((d,A)=>{let l=X1(d.at,H,M),Z=F1(d.value,C);return`${A===0?"M":"L"}${l.toFixed(2)},${Z.toFixed(2)}`}).join(" ")}}function H2(L,V,C){let H=J1(L,V);if(H.length===0)return null;let M=new Date(C).getTime(),e=H[0];for(let i of H){if(new Date(i.at).getTime()>M)break;e=i}return e.value}function r1(L){return new Date(L).toLocaleString(void 0,{month:"short",day:"numeric",hour:"numeric",minute:"2-digit"})}function Y(L){return`${Math.round(L*10)/10}\xB0`}var u5=["current_temperature","temperature"];function V2(L){let V=L.getFullYear(),C=String(L.getMonth()+1).padStart(2,"0"),H=String(L.getDate()).padStart(2,"0");return`${V}-${C}-${H}`}function S5(){return V2(new Date)}function h5(){let L=new Date;return L.setDate(L.getDate()-6),V2(L)}function q2(L){let V=L.replace(/_/g," ");return V.charAt(0).toUpperCase()+V.slice(1)}function g5(L,V){if(L==="climate"){let C=[],H=V.attributes.temperature,M=V.attributes.current_temperature,e=V.attributes.hvac_action;return typeof H=="number"&&C.push(`Target ${Y(H)}`),typeof M=="number"&&C.push(`Room ${Y(M)}`),typeof e=="string"&&e&&C.push(q2(e)),C.join(" \xB7 ")}if(L==="light"){let C=V.attributes.brightness;return typeof C=="number"?`Brightness ${Math.round(C/255*100)}%`:""}return""}function f5(L){return L.source==="rule"?`Scheduler+: ${L.rule_name} (${L.schedule_name})`:"Other"}var O5={climate:L=>`${L.state}|${L.attributes.temperature}|${L.attributes.hvac_action}`};function y5(L,V){let C=O5[V];if(!C)return L;let H=[],M;for(let e of L){let i=C(e);(H.length===0||i!==M)&&(H.push(e),M=i)}return H}function b5(L){let V=new Map;for(let C of L){let H=new Date(C.at),M=V2(H),e=V.get(M)??[];e.push(C),V.set(M,e)}return[...V.entries()].map(([C,H])=>({key:C,label:new Intl.DateTimeFormat(void 0,{weekday:"long",month:"long",day:"numeric",year:"numeric"}).format(new Date(`${C}T12:00:00`)),points:H}))}var f=class extends m{constructor(){super(...arguments);this._entities=[];this._startDate=h5();this._endDate=S5();this._loading=!1;this._handleEntitiesChanged=C=>{this._entities=C.detail.value};this._handleStartDateChange=C=>{this._startDate=C.target.value};this._handleEndDateChange=C=>{this._endDate=C.target.value};this._generate=async()=>{if(!this._canGenerate){this._error=this._entities.length===0?"Pick at least one entity.":"Start date must not be after end date.";return}this._loading=!0,this._error=void 0;try{this._report=await T2(this.hass,this._entities,this._startDate,this._endDate)}catch(C){this._error=C instanceof Error?C.message:String(C)}finally{this._loading=!1}};this._downloadPdf=async()=>{if(!this._canGenerate)return;let C={},H=this.hass.auth?.data?.access_token;H&&(C.Authorization=`Bearer ${H}`);let M=await fetch(B2(this._entities,this._startDate,this._endDate),{headers:C});if(!M.ok)throw new Error(`PDF download failed (${M.status} ${M.statusText})`);let e=await M.blob(),i=URL.createObjectURL(e),d=document.createElement("a");d.href=i,d.download="scheduler-plus-report.pdf",d.click(),URL.revokeObjectURL(i)};this._handleChartMouseMove=(C,H)=>{let M=H.currentTarget.getBoundingClientRect();if(M.width===0)return;let e=Math.min(100,Math.max(0,(H.clientX-M.left)/M.width*100)),i=z2(e,this._startDate,this._endDate);this._hover={entityId:C.entity_id,xPct:e,time:i,actual:H2(C.points,"current_temperature",i),target:H2(C.points,"temperature",i)}};this._handleChartMouseLeave=()=>{this._hover=void 0}}static getStubConfig(){return{type:"custom:scheduler-plus-report-card"}}static getConfigElement(){return document.createElement("scheduler-plus-report-card-editor")}setConfig(C){this._config=C,this._entities=C.entities??[]}getCardSize(){return 6}get _canGenerate(){return this._entities.length>0&&!!this._startDate&&!!this._endDate&&this._startDate<=this._endDate}render(){return t`
      <ha-card>
        <div class="header">
          <span>${this._config?.title??"Report"}</span>
        </div>
        <div class="content">
          <scheduler-plus-entity-multi-picker
            .hass=${this.hass}
            .value=${this._entities}
            .domains=${L1}
            @value-changed=${this._handleEntitiesChanged}
          ></scheduler-plus-entity-multi-picker>

          <div class="controls">
            <div class="control">
              <label class="field-label" for="report-start-date">From</label>
              <input
                id="report-start-date"
                type="date"
                class="native-input"
                .value=${this._startDate}
                @change=${this._handleStartDateChange}
              />
            </div>
            <div class="control">
              <label class="field-label" for="report-end-date">To</label>
              <input
                id="report-end-date"
                type="date"
                class="native-input"
                .value=${this._endDate}
                @change=${this._handleEndDateChange}
              />
            </div>
          </div>

          <div class="actions-row">
            <button
              type="button"
              class="btn btn-primary"
              ?disabled=${this._loading}
              @click=${this._generate}
            >
              ${this._loading?"Generating\u2026":"Generate"}
            </button>
            <button type="button" class="btn" @click=${this._downloadPdf}>Download PDF</button>
          </div>

          ${this._error?t`<div class="placeholder error">${this._error}</div>`:o}

          ${this._renderResults()}
        </div>
      </ha-card>
    `}_renderResults(){return this._report?this._report.entities.length===0?t`<div class="placeholder">No entities selected.</div>`:t`
      <div class="entities">
        ${this._report.entities.map(C=>this._renderEntity(C))}
      </div>
    `:o}_renderEntity(C){return t`
      <div class="entity-report">
        <div class="entity-title">${C.friendly_name}</div>
        ${C.no_data?t`<div class="placeholder small">
              No data found - may be outside your Home Assistant history retention.
            </div>`:t`
              ${this._renderChart(C)}
              ${C.truncated?t`<div class="hint">Truncated - too many changes to list them all.</div>`:o}
              <ul class="points">
                ${b5(y5(C.points,C.domain)).map(H=>t`
                    <li class="day-group">
                      <div class="day-header"><span>${H.label}</span><span>${H.points.length} changes</span></div>
                      <ul class="day-points">
                        ${H.points.map(M=>t`
                          <li class="point">
                            <span class="point-time">${r1(M.at)}</span>
                            <span class="point-state">${q2(M.state)}</span>
                            <span class="point-details">${g5(C.domain,M)}</span>
                            <span class="point-source ${M.source}">${f5(M)}</span>
                          </li>
                        `)}
                      </ul>
                    </li>
                  `)}
              </ul>
            `}
      </div>
    `}_renderChart(C){return C.domain==="climate"?this._renderNumericChart(C):this._renderTimeline(C)}_renderNumericChart(C){let H=K2(C.points,[...u5]);if(!H)return o;let M=C2(C.points,"current_temperature",H,this._startDate,this._endDate),e=C2(C.points,"temperature",H,this._startDate,this._endDate),i=this._hover?.entityId===C.entity_id?this._hover:void 0,d=i?.actual!=null?F1(i.actual,H):void 0,A=i?.target!=null?F1(i.target,H):void 0;return t`
      <div class="chart">
        <div class="chart-body">
          <div class="y-axis">
            <span>${Y(H.max)}</span>
            <span>${Y(H.min)}</span>
          </div>
          <div
            class="chart-plot"
            @mousemove=${l=>this._handleChartMouseMove(C,l)}
            @mouseleave=${this._handleChartMouseLeave}
          >
            <svg viewBox="0 0 100 100" preserveAspectRatio="none">
              <line class="gridline" x1="0" y1="25" x2="100" y2="25"></line>
              <line class="gridline" x1="0" y1="50" x2="100" y2="50"></line>
              <line class="gridline" x1="0" y1="75" x2="100" y2="75"></line>
              ${M?H1`<path class="series actual" d=${M.path}></path>`:o}
              ${e?H1`<path class="series target" d=${e.path}></path>`:o}
              ${i?H1`
                    <line
                      class="hover-guide"
                      x1=${i.xPct} y1="0" x2=${i.xPct} y2="100"
                    ></line>
                    ${d!==void 0?H1`<circle class="hover-dot actual" cx=${i.xPct} cy=${d} r="2"></circle>`:o}
                    ${A!==void 0?H1`<circle class="hover-dot target" cx=${i.xPct} cy=${A} r="2"></circle>`:o}
                  `:o}
            </svg>
            ${i?t`
                  <div
                    class="chart-tooltip"
                    style="left: ${Math.min(88,Math.max(12,i.xPct))}%"
                  >
                    <div class="chart-tooltip-time">${r1(i.time)}</div>
                    ${i.actual!=null?t`<div class="chart-tooltip-row actual">
                          Actual: ${Y(i.actual)}
                        </div>`:o}
                    ${i.target!=null?t`<div class="chart-tooltip-row target">
                          Target: ${Y(i.target)}
                        </div>`:o}
                  </div>
                `:o}
          </div>
        </div>
        <div class="legend">
          ${M?t`<span class="legend-item actual">Actual temperature</span>`:o}
          ${e?t`<span class="legend-item target">Target temperature</span>`:o}
        </div>
      </div>
    `}_renderTimeline(C){let H=Q2(C.points,this._startDate,this._endDate);return t`
      <div class="timeline" title="${this._startDate} to ${this._endDate}">
        ${H.map(M=>t`
            <span
              class="timeline-bar"
              style="left: ${M.leftPct}%; width: ${M.widthPct}%"
              title=${M.title}
            ></span>
          `)}
      </div>
    `}};f.styles=v`
    .header {
      display: flex;
      align-items: center;
      gap: 10px;
      padding: 16px 16px 4px;
      font-size: 1.25rem;
      font-weight: 500;
      color: var(--primary-text-color);
    }
    .content {
      display: flex;
      flex-direction: column;
      gap: 16px;
      padding: 8px 16px 16px;
    }
    .controls {
      display: flex;
      gap: 12px;
    }
    .control {
      flex: 1;
      display: flex;
      flex-direction: column;
      gap: 4px;
    }
    .field-label {
      font-size: 0.85em;
      color: var(--secondary-text-color);
    }
    .native-input {
      font: inherit;
      color: var(--primary-text-color);
      background: var(--card-background-color);
      border: 1px solid var(--divider-color);
      border-radius: 4px;
      padding: 8px;
    }
    .actions-row {
      display: flex;
      gap: 8px;
    }
    .placeholder {
      padding: 16px 0;
      color: var(--secondary-text-color);
      text-align: center;
    }
    .placeholder.small {
      padding: 8px 0;
      font-size: 0.85em;
    }
    .placeholder.error {
      color: var(--error-color);
      padding: 4px 0;
      text-align: left;
    }
    .hint {
      font-size: 0.8em;
      color: var(--secondary-text-color);
      font-style: italic;
    }
    .entities {
      display: flex;
      flex-direction: column;
      gap: 18px;
    }
    .entity-report {
      border: 1px solid var(--divider-color);
      border-radius: 8px;
      padding: 10px 12px;
    }
    .entity-title {
      font-size: 0.95em;
      font-weight: 600;
      color: var(--primary-text-color);
      margin-bottom: 8px;
    }
    .timeline {
      position: relative;
      height: 10px;
      margin-bottom: 10px;
      border-radius: 4px;
      background: var(--secondary-background-color, rgba(0, 0, 0, 0.06));
      overflow: hidden;
    }
    .timeline-bar {
      position: absolute;
      top: 0;
      bottom: 0;
      min-width: 2px;
      background: var(--primary-color);
      opacity: 0.85;
    }
    .chart {
      margin-bottom: 10px;
    }
    .chart-body {
      display: flex;
      gap: 6px;
    }
    .y-axis {
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      align-items: flex-end;
      flex: none;
      width: 40px;
      padding: 4px 0;
      font-size: 0.7em;
      color: var(--secondary-text-color);
    }
    .chart-plot {
      position: relative;
      flex: 1;
      min-width: 0;
    }
    .chart-plot svg {
      width: 100%;
      height: 130px;
      display: block;
      background: var(--secondary-background-color, rgba(0, 0, 0, 0.04));
      border-radius: 4px;
      cursor: crosshair;
    }
    .gridline {
      stroke: var(--divider-color);
      stroke-width: 0.5;
      vector-effect: non-scaling-stroke;
    }
    .series {
      fill: none;
      stroke-width: 2;
      vector-effect: non-scaling-stroke;
    }
    .series.actual {
      stroke: var(--primary-color);
    }
    .series.target {
      stroke: var(--warning-color, #ffa600);
      stroke-dasharray: 4 3;
    }
    .hover-guide {
      stroke: var(--secondary-text-color);
      stroke-width: 1;
      vector-effect: non-scaling-stroke;
      opacity: 0.6;
    }
    .hover-dot {
      vector-effect: non-scaling-stroke;
      stroke: var(--card-background-color);
      stroke-width: 1;
    }
    .hover-dot.actual {
      fill: var(--primary-color);
    }
    .hover-dot.target {
      fill: var(--warning-color, #ffa600);
    }
    .chart-tooltip {
      position: absolute;
      top: 6px;
      transform: translateX(-50%);
      background: var(--card-background-color);
      border: 1px solid var(--divider-color);
      border-radius: 6px;
      padding: 6px 10px;
      font-size: 0.78em;
      white-space: nowrap;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.2);
      pointer-events: none;
    }
    .chart-tooltip-time {
      color: var(--secondary-text-color);
      margin-bottom: 2px;
    }
    .chart-tooltip-row.actual {
      color: var(--primary-color);
      font-weight: 600;
    }
    .chart-tooltip-row.target {
      color: var(--warning-color, #ffa600);
      font-weight: 600;
    }
    .legend {
      display: flex;
      gap: 12px;
      margin-top: 4px;
      font-size: 0.75em;
      color: var(--secondary-text-color);
    }
    .legend-item.actual::before,
    .legend-item.target::before {
      content: "";
      display: inline-block;
      width: 10px;
      height: 2px;
      margin-right: 4px;
      vertical-align: middle;
    }
    .legend-item.actual::before {
      background: var(--primary-color);
    }
    .legend-item.target::before {
      background: var(--warning-color, #ffa600);
    }
    ul.points {
      list-style: none;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      gap: 4px;
      max-height: 260px;
      overflow-y: auto;
    }
    .day-group {
      margin: 2px 0 8px;
    }
    .day-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 7px 8px;
      border-left: 4px solid var(--primary-color);
      border-bottom: 1px solid var(--divider-color);
      background: var(--secondary-background-color, rgba(0, 0, 0, 0.06));
      color: var(--primary-text-color);
      font-size: 0.82em;
      font-weight: 700;
    }
    .day-header span:last-child {
      color: var(--secondary-text-color);
      font-size: 0.9em;
      font-weight: 500;
    }
    ul.day-points {
      list-style: none;
      margin: 4px 0 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      gap: 4px;
    }
    .point {
      display: grid;
      grid-template-columns: 96px 48px 1fr auto;
      gap: 8px;
      align-items: baseline;
      padding: 4px 6px;
      border-radius: 4px;
      font-size: 0.8em;
      background: var(--secondary-background-color, rgba(0, 0, 0, 0.03));
    }
    .point-time {
      color: var(--secondary-text-color);
      white-space: nowrap;
    }
    .point-state {
      font-weight: 600;
      color: var(--primary-text-color);
    }
    .point-details {
      color: var(--secondary-text-color);
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }
    .point-source {
      font-size: 0.9em;
      white-space: nowrap;
      color: var(--secondary-text-color);
    }
    .point-source.rule {
      color: var(--primary-color);
    }
    .btn {
      font: inherit;
      font-weight: 500;
      font-size: 14px;
      padding: 8px 16px;
      border-radius: 6px;
      border: 1px solid var(--divider-color);
      background: var(--card-background-color);
      color: var(--primary-text-color);
      cursor: pointer;
    }
    .btn:hover {
      background: var(--secondary-background-color, rgba(0, 0, 0, 0.06));
    }
    .btn:disabled {
      opacity: 0.5;
      cursor: default;
    }
    .btn-primary {
      background: var(--primary-color);
      border-color: var(--primary-color);
      color: var(--text-primary-color, #fff);
    }
    .btn-primary:hover {
      filter: brightness(0.95);
    }
  `,r([n({attribute:!1})],f.prototype,"hass",2),r([a()],f.prototype,"_config",2),r([a()],f.prototype,"_entities",2),r([a()],f.prototype,"_startDate",2),r([a()],f.prototype,"_endDate",2),r([a()],f.prototype,"_loading",2),r([a()],f.prototype,"_error",2),r([a()],f.prototype,"_report",2),r([a()],f.prototype,"_hover",2),f=r([x("scheduler-plus-report-card")],f);window.customCards=window.customCards??[];window.customCards.push({type:"scheduler-plus-report-card",name:"Scheduler+ Report",description:"History report for any entities and date range, with PDF export."});var X=class extends m{constructor(){super(...arguments);this._handleTitleChange=C=>{let H=C.target.value;this._fireConfigChanged({...this._config,title:H||void 0})}}setConfig(C){this._config=C}_fireConfigChanged(C){this._config=C,this.dispatchEvent(new CustomEvent("config-changed",{detail:{config:C}}))}render(){return this._config?t`
      <div class="editor">
        <label class="field-label" for="card-title">Title</label>
        <input
          id="card-title"
          type="text"
          class="native-input"
          .value=${this._config.title??""}
          @input=${this._handleTitleChange}
        />

        <label class="field-label">Default entities</label>
        <span class="hint">
          Leave empty to let whoever opens this card pick entities freely
          each time. Otherwise, the entity picker starts pre-filled with
          these (still changeable per report).
        </span>
        <scheduler-plus-entity-multi-picker
          .hass=${this.hass}
          .value=${this._config.entities??[]}
          .domains=${L1}
          @value-changed=${C=>{this._fireConfigChanged({...this._config,entities:C.detail.value})}}
        ></scheduler-plus-entity-multi-picker>
      </div>
    `:t``}};X.styles=v`
    .editor {
      display: flex;
      flex-direction: column;
      gap: 8px;
      padding: 8px 0;
    }
    .field-label {
      font-size: 0.85em;
      color: var(--secondary-text-color);
    }
    .hint {
      font-size: 0.85em;
      color: var(--secondary-text-color);
    }
    .native-input {
      font: inherit;
      color: var(--primary-text-color);
      background: var(--card-background-color);
      border: 1px solid var(--divider-color);
      border-radius: 4px;
      padding: 8px;
    }
  `,r([n({attribute:!1})],X.prototype,"hass",2),r([a()],X.prototype,"_config",2),X=r([x("scheduler-plus-report-card-editor")],X);var T=class extends m{constructor(){super(...arguments);this._open=!1;this._templates=[];this._loading=!1;this._closeDialog=()=>{this._open=!1};this._pick=C=>{let H=C.rules[0];H&&(this._onPick?.(H),this._open=!1)};this._deleteTemplateRow=async C=>{if(window.confirm(`Delete template "${C.name}"?`))try{await y1(this.hass,C.id),await this._load()}catch(H){window.alert(H instanceof Error?H.message:String(H))}}}showDialog(C,H){this._deviceType=C,this._onPick=H,this._error=void 0,this._open=!0,this._load()}async _load(){this._loading=!0,this._error=void 0;try{let C=await O1(this.hass);this._templates=C.filter(H=>H.scope==="rule"&&H.device_type===this._deviceType)}catch(C){this._error=C instanceof Error?C.message:String(C)}finally{this._loading=!1}}render(){return this._open?t`
      <ha-dialog open @closed=${this._closeDialog}>
        <div class="form">
          <div class="dialog-title">Start from template</div>
          ${this._error?t`<div class="error">${this._error}</div>`:o}
          ${this._renderContent()}
          <div class="dialog-actions">
            <button type="button" class="btn" @click=${this._closeDialog}>Close</button>
          </div>
        </div>
      </ha-dialog>
    `:o}_renderContent(){return this._loading?t`<div class="placeholder">Loading templates…</div>`:this._templates.length===0?t`
        <div class="placeholder">
          No rule templates saved yet for this device type. Save one from a
          rule's editor with "Save as template".
        </div>
      `:t`
      <ul class="templates">
        ${this._templates.map(C=>t`
            <li class="template">
              <span class="template-name">${C.name}</span>
              <div class="row-actions">
                <button type="button" class="btn" @click=${()=>this._pick(C)}>
                  Use
                </button>
                <ha-icon-button
                  .path=${E}
                  label="Delete template"
                  @click=${()=>this._deleteTemplateRow(C)}
                ></ha-icon-button>
              </div>
            </li>
          `)}
      </ul>
    `}};T.styles=v`
    .form {
      display: flex;
      flex-direction: column;
      gap: 12px;
      min-width: 280px;
      max-width: 400px;
    }
    .dialog-title {
      font-size: 1.25rem;
      font-weight: 500;
      color: var(--primary-text-color);
    }
    .error {
      color: var(--error-color);
    }
    .placeholder {
      padding: 16px 0;
      color: var(--secondary-text-color);
      text-align: center;
    }
    ul.templates {
      list-style: none;
      margin: 0;
      padding: 0;
    }
    .template {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
      padding: 8px 0;
      border-bottom: 1px solid var(--divider-color);
    }
    .template:last-child {
      border-bottom: none;
    }
    .template-name {
      font-weight: 500;
    }
    .row-actions {
      display: flex;
      align-items: center;
      gap: 4px;
    }
    .dialog-actions {
      display: flex;
      justify-content: flex-end;
      gap: 8px;
      padding-top: 8px;
      border-top: 1px solid var(--divider-color);
    }
    .btn {
      font: inherit;
      font-weight: 500;
      font-size: 14px;
      padding: 8px 16px;
      border-radius: 6px;
      border: 1px solid var(--divider-color);
      background: var(--card-background-color);
      color: var(--primary-text-color);
      cursor: pointer;
    }
    .btn:hover {
      background: var(--secondary-background-color, rgba(0, 0, 0, 0.06));
    }
  `,r([n({attribute:!1})],T.prototype,"hass",2),r([a()],T.prototype,"_open",2),r([a()],T.prototype,"_templates",2),r([a()],T.prototype,"_loading",2),r([a()],T.prototype,"_error",2),T=r([x("scheduler-plus-rule-template-picker")],T);var B=class extends m{constructor(){super(...arguments);this._open=!1;this._name="";this._saving=!1;this._rules=[];this._scope="schedule";this._closeDialog=()=>{this._open=!1};this._save=async()=>{let C=this._name.trim();if(!C){this._error="Name is required.";return}if(this._rules.length===0){this._error="Add at least one rule before saving as a template.";return}if(this._deviceType){this._saving=!0,this._error=void 0;try{await w2(this.hass,{name:C,device_type:this._deviceType,rules:this._rules,scope:this._scope}),this._open=!1}catch(H){this._error=H instanceof Error?H.message:String(H)}finally{this._saving=!1}}}}showDialog(C,H,M){this._deviceType=C,this._rules=H,this._scope=M,this._name="",this._error=void 0,this._open=!0}render(){return this._open?t`
      <ha-dialog open @closed=${this._closeDialog}>
        <div class="form">
          <div class="dialog-title">
            ${this._scope==="rule"?"Save rule as template":"Save schedule as template"}
          </div>
          <span class="hint">
            ${this._scope==="rule"?'Saves this one rule as a reusable template - no specific entities - so it can be added to another schedule later via "Start from template".':`Saves this schedule's device type and rules as a reusable template - no specific entities - so another schedule can be built from the same setup later via "From template".`}
          </span>
          ${this._error?t`<div class="error">${this._error}</div>`:o}

          <label class="field-label" for="template-name">Template name</label>
          <input
            id="template-name"
            type="text"
            class="native-input"
            placeholder="Standard Classroom Weekday"
            .value=${this._name}
            @input=${C=>{this._name=C.target.value}}
          />

          <div class="dialog-actions">
            <button type="button" class="btn" @click=${this._closeDialog}>Cancel</button>
            <button
              type="button"
              class="btn btn-primary"
              ?disabled=${this._saving}
              @click=${this._save}
            >
              Save template
            </button>
          </div>
        </div>
      </ha-dialog>
    `:o}};B.styles=v`
    .form {
      display: flex;
      flex-direction: column;
      gap: 12px;
      min-width: 320px;
      max-width: 420px;
    }
    .dialog-title {
      font-size: 1.25rem;
      font-weight: 500;
      color: var(--primary-text-color);
    }
    .hint {
      font-size: 0.85em;
      color: var(--secondary-text-color);
    }
    .error {
      color: var(--error-color);
    }
    .field-label {
      font-size: 0.85em;
      color: var(--secondary-text-color);
    }
    .native-input {
      font: inherit;
      color: var(--primary-text-color);
      background: var(--card-background-color);
      border: 1px solid var(--divider-color);
      border-radius: 4px;
      padding: 8px;
    }
    .dialog-actions {
      display: flex;
      justify-content: flex-end;
      gap: 8px;
      padding-top: 8px;
      border-top: 1px solid var(--divider-color);
    }
    .btn {
      font: inherit;
      font-weight: 500;
      font-size: 14px;
      padding: 8px 16px;
      border-radius: 6px;
      border: 1px solid var(--divider-color);
      background: var(--card-background-color);
      color: var(--primary-text-color);
      cursor: pointer;
    }
    .btn:hover {
      background: var(--secondary-background-color, rgba(0, 0, 0, 0.06));
    }
    .btn:disabled {
      opacity: 0.5;
      cursor: default;
    }
    .btn-primary {
      background: var(--primary-color);
      border-color: var(--primary-color);
      color: var(--text-primary-color, #fff);
    }
    .btn-primary:hover {
      filter: brightness(0.95);
    }
  `,r([n({attribute:!1})],B.prototype,"hass",2),r([a()],B.prototype,"_open",2),r([a()],B.prototype,"_name",2),r([a()],B.prototype,"_saving",2),r([a()],B.prototype,"_error",2),B=r([x("scheduler-plus-template-editor")],B);var k5={weekday_days:["mon","tue","wed","thu","fri"],weekend_days:["sat","sun"],working_hours_start:"09:00",working_hours_end:"17:00",enable_brightness:!0,enable_fade_in:!0},L2=[{key:"fixed",label:K.fixed,makeSpec:()=>({provider:"fixed",params:{time:"06:00"}}),matches:L=>L.provider==="fixed"},{key:"sunrise",label:K.sunrise,makeSpec:()=>({provider:"sunrise",params:{offset_minutes:0}}),matches:L=>L.provider==="sunrise"},{key:"sunset",label:K.sunset,makeSpec:()=>({provider:"sunset",params:{offset_minutes:0}}),matches:L=>L.provider==="sunset"},...R2.map(L=>({key:`yidcal:${L}`,label:w1[L],makeSpec:()=>({provider:"yidcal",params:{zman:L,offset_minutes:0}}),matches:V=>V.provider==="yidcal"&&V.params.zman===L}))];function t1(L){let V=new Date(`${L}T00:00:00`);return Number.isNaN(V.getTime())?L:V.toLocaleDateString(void 0,{month:"short",day:"numeric",year:"numeric"})}var p=class extends m{constructor(){super(...arguments);this._open=!1;this._preferences=k5;this._deviceType="light";this._name="";this._enabled=!0;this._days=[];this._dateMode="always";this._dates=[];this._newDate="";this._dateRanges=[];this._newRangeStart="";this._newRangeEnd="";this._dayConditions=[];this._onTime={provider:"fixed",params:{time:"06:00"}};this._offTime={provider:"fixed",params:{time:"21:00"}};this._onEnabled=!0;this._offEnabled=!0;this._setBrightness=!1;this._brightnessPct=100;this._useTransition=!1;this._transitionSeconds=0;this._hvacMode="heat";this._useTargetTemperature=!1;this._targetTemperature=70;this._useSetback=!1;this._setbackHvacMode="heat";this._setbackTemperature=78;this._allowOverride=!0;this._overrideGraceMinutes=15;this._additionalActions=[];this._applyRuleTemplate=C=>{this._hydrateFromRule(C)};this._openTemplatePicker=()=>{this._templatePicker?.showDialog(this._deviceType,this._applyRuleTemplate)};this._closeDialog=()=>{this._open=!1};this._toggleDay=C=>{this._days=this._days.includes(C)?this._days.filter(H=>H!==C):[...this._days,C]};this._applyDayPreset=C=>{this._days=[...C]};this._applyAfterHoursPreset=()=>{this._days=[...b],this._onTime={provider:"fixed",params:{time:this._preferences.working_hours_end.slice(0,5)}},this._offTime={provider:"fixed",params:{time:this._preferences.working_hours_start.slice(0,5)}}};this._handleDateModeChange=C=>{let H=C.target.value;this._dateMode=H,H==="include"?this._days=[...b]:H==="always"&&(this._dates=[],this._dateRanges=[],this._dayConditions=[])};this._addDate=()=>{!this._newDate||this._dates.includes(this._newDate)||(this._dates=[...this._dates,this._newDate].sort(),this._newDate="")};this._removeDate=C=>{this._dates=this._dates.filter(H=>H!==C)};this._addDateRange=()=>{!this._newRangeStart||!this._newRangeEnd||this._newRangeStart>this._newRangeEnd||(this._dateRanges=[...this._dateRanges,[this._newRangeStart,this._newRangeEnd]],this._newRangeStart="",this._newRangeEnd="")};this._removeDateRange=C=>{this._dateRanges=this._dateRanges.filter(H=>H[0]!==C[0]||H[1]!==C[1])};this._toggleDayCondition=C=>{this._dayConditions=this._dayConditions.includes(C)?this._dayConditions.filter(H=>H!==C):[...this._dayConditions,C]};this._addAdditionalAction=()=>{this._additionalActions=[...this._additionalActions,{}]};this._removeAdditionalAction=C=>{this._additionalActions=this._additionalActions.filter((H,M)=>M!==C)};this._updateAdditionalAction=(C,H)=>{try{let M=JSON.parse(H);M&&typeof M=="object"&&!Array.isArray(M)&&(this._additionalActions=this._additionalActions.map((e,i)=>i===C?M:e))}catch{}};this._updateAdditionalActionField=(C,H,M)=>{this._additionalActions=this._additionalActions.map((e,i)=>i===C?{...e,[H]:M}:e)};this._save=()=>{let C=this._validate();if(C){this._error=C;return}this._onSave?.(this._buildRuleInput()),this._open=!1};this._openSaveAsTemplate=()=>{let C=this._validate();if(C){this._error=C;return}this._templateEditor?.showDialog(this._deviceType,[this._buildRuleInput()],"rule")}}showDialog(C){let{deviceType:H,rule:M,onSave:e}=C;this._deviceType=H,this._rule=M,this._onSave=e,this._loadPreferences(),this._hydrateFromRule(M),this._error=void 0,this._open=!0}_hydrateFromRule(C){if(this._name=C?.name??"",this._enabled=C?.enabled??!0,this._days=C?[...C.days]:[],this._dateMode=C?.date_mode??"always",this._dates=C?[...C.dates]:[],this._newDate="",this._dateRanges=C?C.date_ranges.map(([H,M])=>[H,M]):[],this._newRangeStart="",this._newRangeEnd="",this._dayConditions=C?[...C.day_conditions]:[],this._onTime=C?.on_time??{provider:"fixed",params:{time:"06:00"}},this._offTime=C?.off_time??{provider:"fixed",params:{time:"21:00"}},this._onEnabled=C?.on_enabled??!0,this._offEnabled=C?.off_enabled??!0,this._allowOverride=C?.allow_override??!0,this._overrideGraceMinutes=C?.override_grace_minutes??15,this._additionalActions=(C?.actions??[]).slice(1).map(H=>({...H})),this._deviceType==="light"||this._deviceType==="light_switch"){this._setBrightness=C?.action.brightness!==void 0;let H=C?.action.brightness??255;this._brightnessPct=Math.round(H/255*100),this._useTransition=C?.action.transition!==void 0,this._transitionSeconds=C?.action.transition??0}else this._deviceType==="climate"&&(this._hvacMode=C?.action.hvac_mode??"heat",this._useTargetTemperature=C?.action.target_temperature!==void 0,this._targetTemperature=C?.action.target_temperature??70,this._useSetback=!!C?.off_action,this._setbackHvacMode=C?.off_action?.hvac_mode??"heat",this._setbackTemperature=C?.off_action?.target_temperature??78)}async _loadPreferences(){try{this._preferences=await f1(this.hass)}catch{}}_summarizeDateFilter(){let C=[...this._dates.map(H=>t1(H)),...this._dateRanges.map(([H,M])=>`${t1(H)}\u2013${t1(M)}`),...this._dayConditions.map(H=>m1[H])];return this._dateMode==="include"?C.length===0?"Nothing selected yet - as configured, this rule will never run.":`Runs only when it's ${C.join(", ")} - the Days above are ignored.`:C.length===0?"Nothing excluded yet - this behaves the same as \u201CAlways\u201D.":`Runs on the Days above as usual, except when it's ${C.join(", ")}.`}_renderAdditionalAction(C,H){return this._deviceType==="climate"?t`
        <label class="field-label">HVAC mode</label>
        <select class="native-select" .value=${String(C.hvac_mode??"heat")}
          @change=${M=>this._updateAdditionalActionField(H,"hvac_mode",M.target.value)}>
          ${_1.map(M=>t`<option value=${M}>${M1[M]}</option>`)}
        </select>
        <label class="check-row">
          <input type="checkbox" .checked=${C.target_temperature!==void 0}
            @change=${M=>{let e=M.target.checked;this._additionalActions=this._additionalActions.map((i,d)=>{if(d!==H)return i;let A={...i};return e?A.target_temperature=70:delete A.target_temperature,A})}} /> Set temperature
        </label>
        ${C.target_temperature!==void 0?t`
          <input class="native-input" type="number" .value=${String(C.target_temperature)}
            @input=${M=>this._updateAdditionalActionField(H,"target_temperature",Number(M.target.value))} />
        `:o}
      `:this._deviceType==="light"||this._deviceType==="light_switch"?t`
        <label class="check-row"><input type="checkbox" .checked=${C.brightness!==void 0}
          @change=${M=>this._updateAdditionalActionField(H,"brightness",M.target.checked?255:void 0)} /> Set brightness</label>
        ${C.brightness!==void 0?t`<input class="native-input" type="range" min="1" max="255" .value=${String(C.brightness)}
          @input=${M=>this._updateAdditionalActionField(H,"brightness",Number(M.target.value))} />`:o}
        <label class="check-row"><input type="checkbox" .checked=${C.transition!==void 0}
          @change=${M=>this._updateAdditionalActionField(H,"transition",M.target.checked?0:void 0)} /> Fade in</label>
        ${C.transition!==void 0?t`<input class="native-input" type="number" min="0" .value=${String(C.transition)}
          @input=${M=>this._updateAdditionalActionField(H,"transition",Number(M.target.value))} />`:o}
      `:t`<span class="hint">This action uses the selected switch entities.</span>`}_validate(){return this._name.trim()?this._dateMode!=="include"&&this._days.length===0?"At least one day is required.":this._dateMode!=="always"&&this._dates.length===0&&this._dateRanges.length===0&&this._dayConditions.length===0?"At least one date, date range, or special condition is required.":!this._onEnabled&&!this._offEnabled?"At least one of On time or Off time must be enabled.":null:"Name is required."}_buildRuleInput(){let C={},H=null;return this._deviceType==="light"||this._deviceType==="light_switch"?C={...this._setBrightness?{brightness:Math.round(this._brightnessPct/100*255)}:{},...this._useTransition?{transition:this._transitionSeconds}:{}}:this._deviceType==="climate"&&(C={hvac_mode:this._hvacMode,...this._useTargetTemperature?{target_temperature:this._targetTemperature}:{}},H=this._useSetback?{hvac_mode:this._setbackHvacMode,target_temperature:this._setbackTemperature}:null),{id:this._rule?.id,name:this._name.trim(),enabled:this._enabled,days:this._days,date_mode:this._dateMode,dates:this._dates,date_ranges:this._dateRanges,day_conditions:this._dayConditions,on_time:this._onTime,off_time:this._offTime,on_enabled:this._onEnabled,off_enabled:this._offEnabled,allow_override:this._allowOverride,override_grace_minutes:this._overrideGraceMinutes,action:C,actions:[C,...this._additionalActions],off_action:H}}render(){return this._open?t`
      <ha-dialog open @closed=${this._closeDialog}>
        <div class="form">
          <div class="dialog-title">${this._rule?"Edit rule":"Add rule"}</div>
          ${this._error?t`<div class="error">${this._error}</div>`:o}

          <button type="button" class="btn" @click=${this._openTemplatePicker}>
            Start from template
          </button>

          <section class="section">
            <label class="field-label" for="rule-name">Name</label>
            <input
              id="rule-name"
              type="text"
              class="native-input"
              .value=${this._name}
              @input=${C=>{this._name=C.target.value}}
            />

            <ha-formfield label="Enabled">
              <ha-switch
                .checked=${this._enabled}
                @change=${C=>{this._enabled=C.target.checked}}
              ></ha-switch>
            </ha-formfield>
          </section>

          <section class="section">
            <h3 class="section-title">When this rule runs</h3>

            <label class="field-label">Days</label>
            <div class="day-presets">
              <button type="button" class="btn" @click=${()=>this._applyDayPreset(b)}>
                Every day
              </button>
              <button
                type="button"
                class="btn"
                @click=${()=>this._applyDayPreset(this._preferences.weekday_days)}
              >
                Weekdays
              </button>
              <button
                type="button"
                class="btn"
                @click=${()=>this._applyDayPreset(this._preferences.weekend_days)}
              >
                Weekend
              </button>
              <button type="button" class="btn" @click=${this._applyAfterHoursPreset}>
                After hours
              </button>
            </div>
            <div class="days">
              ${b.map(C=>t`
                  <button
                    type="button"
                    class="day-chip ${this._days.includes(C)?"active":""}"
                    ?disabled=${this._dateMode==="include"}
                    @click=${()=>this._toggleDay(C)}
                  >
                    ${q[C].slice(0,3)}
                  </button>
                `)}
            </div>
            ${this._dateMode==="include"?t`<span class="hint">Ignored - this rule uses a date filter instead.</span>`:o}

            <label class="field-label" for="date-mode">Date filter</label>
            <select
              id="date-mode"
              class="native-select"
              .value=${this._dateMode}
              @change=${this._handleDateModeChange}
            >
              ${T1.map(C=>t`<option value=${C}>${D2[C]}</option>`)}
            </select>

            ${this._dateMode!=="always"?t`
                  <div class="filter-panel">
                    <p class="filter-summary">${this._summarizeDateFilter()}</p>

                    <label class="panel-label">Specific dates</label>
                    <div class="dates">
                      ${this._dates.map(C=>t`
                          <div class="date-row">
                            <span>${t1(C)}</span>
                            <button
                              type="button"
                              class="btn"
                              @click=${()=>this._removeDate(C)}
                            >
                              Remove
                            </button>
                          </div>
                        `)}
                      <div class="date-row">
                        <input
                          type="date"
                          class="native-input"
                          .value=${this._newDate}
                          @input=${C=>{this._newDate=C.target.value}}
                        />
                        <button type="button" class="btn" @click=${this._addDate}>
                          Add date
                        </button>
                      </div>
                    </div>

                    <label class="panel-label">Date range</label>
                    <div class="dates">
                      ${this._dateRanges.map(C=>t`
                          <div class="date-row">
                            <span>${t1(C[0])} – ${t1(C[1])}</span>
                            <button
                              type="button"
                              class="btn"
                              @click=${()=>this._removeDateRange(C)}
                            >
                              Remove
                            </button>
                          </div>
                        `)}
                      <div class="range-add-row">
                        <input
                          type="date"
                          class="native-input"
                          .value=${this._newRangeStart}
                          @input=${C=>{this._newRangeStart=C.target.value}}
                        />
                        <span class="sep">to</span>
                        <input
                          type="date"
                          class="native-input"
                          .value=${this._newRangeEnd}
                          @input=${C=>{this._newRangeEnd=C.target.value}}
                        />
                        <button type="button" class="btn" @click=${this._addDateRange}>
                          Add range
                        </button>
                      </div>
                    </div>

                    <label class="panel-label">Special conditions (YidCal)</label>
                    <div class="days">
                      ${F2.map(C=>t`
                          <button
                            type="button"
                            class="day-chip ${this._dayConditions.includes(C)?"active":""}"
                            @click=${()=>this._toggleDayCondition(C)}
                          >
                            ${m1[C]}
                          </button>
                        `)}
                    </div>
                    <span class="hint">
                      Reflects YidCal's current state, so this can only be
                      confirmed for today - a future Shabbos/Yom Tov won't
                      show up in "Next event" ahead of time, but the rule
                      still applies correctly once that day arrives.
                    </span>
                  </div>
                `:o}
          </section>

          <section class="section">
            <h3 class="section-title">Time</h3>
            <div class="time-columns">
              ${this._renderTimeFields("On time",this._onTime,C=>this._onTime=C,this._onEnabled,C=>this._onEnabled=C)}
              ${this._renderTimeFields("Off time",this._offTime,C=>this._offTime=C,this._offEnabled,C=>this._offEnabled=C)}
            </div>
          </section>

          <section class="section">
            <h3 class="section-title">Action</h3>
            ${this._renderActionFields()}
            <div class="additional-actions">
              <h4>Additional actions</h4>
              <p class="hint">These run in order on the same schedule entities.</p>
              ${this._additionalActions.map((C,H)=>t`
                  <div class="additional-action-row">
                    <label>Action ${H+2}</label>
                    ${this._renderAdditionalAction(C,H)}
                    <button type="button" class="btn" @click=${()=>this._removeAdditionalAction(H)}>
                      Remove
                    </button>
                  </div>
                `)}
              <button type="button" class="btn" @click=${this._addAdditionalAction}>
                Add action
              </button>
            </div>
          </section>

          <div class="dialog-actions">
            <button type="button" class="btn" @click=${this._openSaveAsTemplate}>
              Save as template
            </button>
            <span class="spacer"></span>
            <button type="button" class="btn" @click=${this._closeDialog}>Cancel</button>
            <button type="button" class="btn btn-primary" @click=${this._save}>Save</button>
          </div>
        </div>
      </ha-dialog>
      <scheduler-plus-template-editor .hass=${this.hass}></scheduler-plus-template-editor>
      <scheduler-plus-rule-template-picker
        .hass=${this.hass}
      ></scheduler-plus-rule-template-picker>
    `:o}_renderTimeFields(C,H,M,e,i){let d=L2.find(A=>A.matches(H))?.key??"fixed";return t`
      <div class="time-field">
        <ha-formfield label=${C}>
          <ha-switch
            .checked=${e}
            @change=${A=>{i(A.target.checked)}}
          ></ha-switch>
        </ha-formfield>
        ${e?t`
              <div class="time-row">
                <select
                  class="native-select"
                  .value=${d}
                  @change=${A=>{let l=A.target.value,Z=L2.find(s=>s.key===l);Z&&M(Z.makeSpec())}}
                >
                  ${L2.map(A=>t`<option value=${A.key}>${A.label}</option>`)}
                </select>
                ${H.provider==="fixed"?t`
                      <input
                        type="time"
                        class="native-input"
                        .value=${H.params.time??""}
                        @input=${A=>M({...H,params:{time:A.target.value}})}
                      />
                    `:t`
                      <input
                        type="number"
                        class="native-input offset"
                        .value=${String(H.params.offset_minutes??0)}
                        @input=${A=>M({...H,params:{...H.params,offset_minutes:Number(A.target.value)||0}})}
                      />
                      <span class="hint">minutes</span>
                    `}
              </div>
            `:o}
      </div>
    `}_renderActionFields(){return this._deviceType==="light"||this._deviceType==="light_switch"?this._renderLightAction():this._deviceType==="climate"?this._renderClimateAction():t`
      <span class="hint">Switches just turn on and off - nothing else to configure.</span>
    `}_renderLightAction(){return t`
      ${this._preferences.enable_brightness?t`
            <ha-formfield label="Set brightness">
              <ha-switch
                .checked=${this._setBrightness}
                @change=${C=>{this._setBrightness=C.target.checked}}
              ></ha-switch>
            </ha-formfield>
            <span class="hint">
              Off by default - the light just turns on at whatever brightness it
              was last set to.
            </span>
            ${this._setBrightness?t`
                  <label class="field-label">Brightness (${this._brightnessPct}%)</label>
                  <input
                    type="range"
                    min="1"
                    max="100"
                    class="native-input"
                    .value=${String(this._brightnessPct)}
                    @input=${C=>{this._brightnessPct=Number(C.target.value)}}
                  />
                `:o}
          `:o}
      ${this._preferences.enable_fade_in?t`
            <ha-formfield label="Fade in gradually">
              <ha-switch
                .checked=${this._useTransition}
                @change=${C=>{this._useTransition=C.target.checked}}
              ></ha-switch>
            </ha-formfield>
            <span class="hint">
              Instead of snapping on instantly, the light ramps up to its target
              level over the given number of seconds.
            </span>
            ${this._useTransition?t`
                  <label class="field-label" for="fade-duration">Fade duration (seconds)</label>
                  <input
                    id="fade-duration"
                    type="number"
                    class="native-input"
                    .value=${String(this._transitionSeconds)}
                    @input=${C=>{this._transitionSeconds=Number(C.target.value)||0}}
                  />
                `:o}
          `:o}
    `}_renderClimateAction(){return t`
      <label class="field-label" for="hvac-mode">HVAC mode</label>
      <select
        id="hvac-mode"
        class="native-select"
        .value=${this._hvacMode}
        @change=${C=>{this._hvacMode=C.target.value}}
      >
        ${_1.map(C=>t`<option value=${C}>${M1[C]}</option>`)}
      </select>

      <ha-formfield label="Set target temperature">
        <ha-switch
          .checked=${this._useTargetTemperature}
          @change=${C=>{this._useTargetTemperature=C.target.checked}}
        ></ha-switch>
      </ha-formfield>
      ${this._useTargetTemperature?t`
            <label class="field-label" for="target-temperature">Target temperature</label>
            <input
              id="target-temperature"
              type="number"
              class="native-input"
              .value=${String(this._targetTemperature)}
              @input=${C=>{this._targetTemperature=Number(C.target.value)||0}}
            />
          `:o}

      <ha-formfield label="Eco setback instead of turning off">
        <ha-switch
          .checked=${this._useSetback}
          @change=${C=>{this._useSetback=C.target.checked}}
        ></ha-switch>
      </ha-formfield>
      <span class="hint">
        ${this._useSetback?"At the Off time below, the thermostat is set to this instead of being turned off - useful for avoiding humidity or freeze issues in an unoccupied space while still saving energy.":"Off by default - the Off time below simply turns the thermostat off."}
      </span>
      ${this._useSetback?t`
            <label class="field-label" for="setback-hvac-mode">Setback HVAC mode</label>
            <select
              id="setback-hvac-mode"
              class="native-select"
              .value=${this._setbackHvacMode}
              @change=${C=>{this._setbackHvacMode=C.target.value}}
            >
              ${_1.map(C=>t`<option value=${C}>${M1[C]}</option>`)}
            </select>

            <label class="field-label" for="setback-temperature">Setback temperature</label>
            <input
              id="setback-temperature"
              type="number"
              class="native-input"
              .value=${String(this._setbackTemperature)}
              @input=${C=>{this._setbackTemperature=Number(C.target.value)||0}}
            />
          `:o}

      <ha-formfield label="Allow override">
        <ha-switch
          .checked=${this._allowOverride}
          @change=${C=>{this._allowOverride=C.target.checked}}
        ></ha-switch>
      </ha-formfield>
      <span class="hint">
        ${this._allowOverride?"Manual changes made directly on the thermostat stick until the next scheduled event.":`If someone changes this away from the rule's setting, Scheduler+ waits ${this._overrideGraceMinutes} minutes and then reapplies it if it still doesn't match.`}
      </span>
      ${this._allowOverride?o:t`
            <label class="field-label" for="override-grace-minutes">
              Grace period (minutes)
            </label>
            <input
              id="override-grace-minutes"
              type="number"
              min="1"
              class="native-input"
              .value=${String(this._overrideGraceMinutes)}
              @input=${C=>{this._overrideGraceMinutes=Number(C.target.value)||1}}
            />
          `}
    `}};p.styles=v`
    .form {
      display: flex;
      flex-direction: column;
      gap: 16px;
      min-width: 320px;
      max-width: 420px;
    }
    .dialog-title {
      font-size: 1.25rem;
      font-weight: 500;
      color: var(--primary-text-color);
    }
    .section {
      display: flex;
      flex-direction: column;
      gap: 10px;
      padding-top: 18px;
      border-top: 1px solid var(--divider-color);
    }
    .section:first-of-type {
      padding-top: 0;
      border-top: none;
    }
    .section-title {
      margin: 0;
      font-size: 0.95rem;
      font-weight: 600;
      color: var(--primary-text-color);
    }
    .filter-panel {
      display: flex;
      flex-direction: column;
      gap: 8px;
      padding: 12px;
      border: 1px solid var(--divider-color);
      border-radius: 8px;
      background: var(--secondary-background-color, rgba(0, 0, 0, 0.03));
    }
    .additional-actions {
      display: flex;
      flex-direction: column;
      gap: 8px;
      padding: 12px;
      border: 1px solid var(--divider-color);
      border-radius: 8px;
    }
    .additional-actions h4 { margin: 0; font-size: 0.9em; }
    .additional-action-row { display: flex; flex-direction: column; gap: 6px; }
    .filter-summary {
      margin: 0 0 4px;
      font-size: 0.9em;
      font-weight: 500;
      color: var(--primary-text-color);
    }
    .panel-label {
      font-size: 0.75em;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.03em;
      color: var(--secondary-text-color);
      margin-top: 6px;
    }
    .panel-label:first-of-type {
      margin-top: 0;
    }
    .range-add-row {
      display: flex;
      align-items: center;
      gap: 6px;
    }
    .range-add-row .native-input {
      flex: 1;
      min-width: 0;
    }
    .range-add-row .sep {
      font-size: 0.85em;
      color: var(--secondary-text-color);
      flex: none;
    }
    .dialog-actions {
      display: flex;
      align-items: center;
      justify-content: flex-end;
      gap: 8px;
      padding-top: 8px;
      border-top: 1px solid var(--divider-color);
    }
    .spacer {
      flex: 1;
    }
    .btn {
      font: inherit;
      font-weight: 500;
      font-size: 14px;
      padding: 8px 16px;
      border-radius: 6px;
      border: 1px solid var(--divider-color);
      background: var(--card-background-color);
      color: var(--primary-text-color);
      cursor: pointer;
    }
    .btn:hover {
      background: var(--secondary-background-color, rgba(0, 0, 0, 0.06));
    }
    .btn:disabled {
      opacity: 0.5;
      cursor: default;
    }
    .btn-primary {
      background: var(--primary-color);
      border-color: var(--primary-color);
      color: var(--text-primary-color, #fff);
    }
    .btn-primary:hover {
      filter: brightness(0.95);
    }
    .error {
      color: var(--error-color);
    }
    .field-label {
      font-size: 0.85em;
      color: var(--secondary-text-color);
    }
    .native-select,
    .native-input {
      font: inherit;
      color: var(--primary-text-color);
      background: var(--card-background-color);
      border: 1px solid var(--divider-color);
      border-radius: 4px;
      padding: 8px;
    }
    .day-presets {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
    }
    .day-presets .btn {
      padding: 6px 12px;
      font-size: 13px;
    }
    .native-input.offset {
      width: 80px;
    }
    .days {
      display: flex;
      flex-wrap: wrap;
      gap: 4px;
    }
    .day-chip {
      font: inherit;
      color: var(--primary-text-color);
      background: var(--card-background-color);
      border: 1px solid var(--divider-color);
      border-radius: 16px;
      padding: 6px 12px;
      cursor: pointer;
    }
    .day-chip.active {
      color: var(--text-primary-color, #fff);
      background: var(--primary-color);
      border-color: var(--primary-color);
    }
    .day-chip:disabled {
      opacity: 0.4;
      cursor: default;
    }
    .dates {
      display: flex;
      flex-direction: column;
      gap: 8px;
    }
    .date-row {
      display: flex;
      align-items: center;
      gap: 8px;
    }
    .date-row span {
      flex: 1;
    }
    .time-columns {
      display: flex;
      flex-wrap: wrap;
      gap: 16px;
    }
    .time-field {
      display: flex;
      flex-direction: column;
      gap: 6px;
      flex: 1 1 200px;
    }
    .time-row {
      display: flex;
      align-items: center;
      gap: 8px;
    }
    .hint {
      font-size: 0.85em;
      color: var(--secondary-text-color);
    }
  `,r([n({attribute:!1})],p.prototype,"hass",2),r([a()],p.prototype,"_open",2),r([a()],p.prototype,"_preferences",2),r([a()],p.prototype,"_deviceType",2),r([a()],p.prototype,"_name",2),r([a()],p.prototype,"_enabled",2),r([a()],p.prototype,"_days",2),r([a()],p.prototype,"_dateMode",2),r([a()],p.prototype,"_dates",2),r([a()],p.prototype,"_newDate",2),r([a()],p.prototype,"_dateRanges",2),r([a()],p.prototype,"_newRangeStart",2),r([a()],p.prototype,"_newRangeEnd",2),r([a()],p.prototype,"_dayConditions",2),r([a()],p.prototype,"_onTime",2),r([a()],p.prototype,"_offTime",2),r([a()],p.prototype,"_onEnabled",2),r([a()],p.prototype,"_offEnabled",2),r([a()],p.prototype,"_setBrightness",2),r([a()],p.prototype,"_brightnessPct",2),r([a()],p.prototype,"_useTransition",2),r([a()],p.prototype,"_transitionSeconds",2),r([a()],p.prototype,"_hvacMode",2),r([a()],p.prototype,"_useTargetTemperature",2),r([a()],p.prototype,"_targetTemperature",2),r([a()],p.prototype,"_useSetback",2),r([a()],p.prototype,"_setbackHvacMode",2),r([a()],p.prototype,"_setbackTemperature",2),r([a()],p.prototype,"_allowOverride",2),r([a()],p.prototype,"_overrideGraceMinutes",2),r([a()],p.prototype,"_additionalActions",2),r([a()],p.prototype,"_error",2),r([O("scheduler-plus-template-editor")],p.prototype,"_templateEditor",2),r([O("scheduler-plus-rule-template-picker")],p.prototype,"_templatePicker",2),p=r([x("scheduler-plus-rule-editor")],p);function _5(L){let[V,C]=L.split(":"),H=Number(V),M=Number(C),e=H>=12?"PM":"AM";return`${H%12===0?12:H%12}:${M.toString().padStart(2,"0")} ${e}`}function E1(L){if(L.provider==="fixed")return _5(L.params.time??"00:00");let V=L.provider==="yidcal"?w1[L.params.zman]??K.yidcal:K[L.provider],C=L.params.offset_minutes??0;return C===0?V:`${V} ${C>0?"+":""}${C}`}function w5(L){return L.on_enabled&&L.off_enabled?`${E1(L.on_time)} \u2192 ${E1(L.off_time)}`:L.on_enabled?`${E1(L.on_time)} only`:`until ${E1(L.off_time)}`}var c=class extends m{constructor(){super(...arguments);this._open=!1;this._name="";this._deviceType="light_switch";this._enabled=!0;this._entities=[];this._rules=[];this._activeDateMode="always";this._activeDateRanges=[];this._newActiveRangeStart="";this._newActiveRangeEnd="";this._saving=!1;this._checkingConflicts=!1;this._conflicts=[];this._closeDialog=()=>{this._open=!1};this._handleDeviceTypeChange=C=>{this._deviceType=C.target.value,this._entities=[],this._rules=[]};this._handleActiveDateModeChange=C=>{this._activeDateMode=C.target.value};this._addActiveDateRange=()=>{let C=this._newActiveRangeStart,H=this._newActiveRangeEnd;!C||!H||C>H||(this._activeDateRanges.some(([M,e])=>M===C&&e===H)||(this._activeDateRanges=[...this._activeDateRanges,[C,H]]),this._newActiveRangeStart="",this._newActiveRangeEnd="")};this._removeActiveDateRange=C=>{this._activeDateRanges=this._activeDateRanges.filter((H,M)=>M!==C)};this._openSaveAsTemplate=()=>{this._templateEditor?.showDialog(this._deviceType,this._rules,"schedule")};this._openAddRuleDialog=()=>{this._ruleEditor?.showDialog({deviceType:this._deviceType,onSave:C=>{this._rules=[...this._rules,C]}})};this._openEditRuleDialog=C=>{this._ruleEditor?.showDialog({deviceType:this._deviceType,rule:this._rules[C],onSave:H=>{this._rules=this._rules.map((M,e)=>e===C?H:M)}})};this._removeRule=C=>{let H=this._rules[C];!H||!window.confirm(`Delete rule "${H.name}"?`)||(this._rules=this._rules.filter((M,e)=>e!==C))};this._toggleRuleEnabled=C=>{this._rules=this._rules.map((H,M)=>M===C?{...H,enabled:!H.enabled}:H)};this._save=async()=>{let C=this._name.trim();if(!C){this._error="Name is required.";return}if(this._entities.length===0){this._error="At least one entity is required.";return}let H=this._buildInput(C);this._checkingConflicts=!0,this._error=void 0;try{let M=await b1(this.hass,this._schedule?.id??null,H);M.length===0?(this._conflicts=[],await this._persist(H)):this._conflicts=M}catch(M){this._error=M instanceof Error?M.message:String(M)}finally{this._checkingConflicts=!1}};this._saveAnyway=()=>{let C=this._name.trim();this._persist(this._buildInput(C))};this._excludeConflict=async C=>{try{await D1(this.hass,C),this._conflicts=this._conflicts.filter(H=>!(H.conflicting_rule_id===C.conflicting_rule_id&&H.date===C.date)),this._conflicts.length===0&&await this._persist(this._buildInput(this._name.trim()))}catch(H){window.alert(H instanceof Error?H.message:String(H))}}}showDialog(C){this._schedule=C,this._name=C?.name??"",this._deviceType=C?.device_type??"light_switch",this._enabled=C?.enabled??!0,this._entities=C?[...C.entities]:[],this._rules=C?C.rules.map(H=>({...H})):[],this._activeDateMode=C?.active_date_mode??"always",this._activeDateRanges=C?.active_date_ranges?[...C.active_date_ranges]:[],this._newActiveRangeStart="",this._newActiveRangeEnd="",this._conflicts=[],this._error=void 0,this._open=!0}showDialogDuplicate(C){this._schedule=void 0,this._name=`Copy of ${C.name}`,this._deviceType=C.device_type,this._enabled=C.enabled,this._entities=[...C.entities],this._rules=C.rules.map(H=>{let{id:M,...e}=H;return{...e}}),this._activeDateMode=C.active_date_mode??"always",this._activeDateRanges=C.active_date_ranges?[...C.active_date_ranges]:[],this._newActiveRangeStart="",this._newActiveRangeEnd="",this._conflicts=[],this._error=void 0,this._open=!0}showDialogFromTemplate(C){this._schedule=void 0,this._name=C.name,this._deviceType=C.device_type,this._enabled=!0,this._entities=[],this._rules=C.rules.map(H=>{let{id:M,...e}=H;return{...e}}),this._activeDateMode="always",this._activeDateRanges=[],this._newActiveRangeStart="",this._newActiveRangeEnd="",this._conflicts=[],this._error=void 0,this._open=!0}_buildInput(C){return{name:C,device_type:this._deviceType,entities:this._entities,enabled:this._enabled,rules:this._rules,active_date_mode:this._activeDateMode,active_date_ranges:this._activeDateRanges,override_until:this._schedule?.override_until??null}}async _persist(C){this._saving=!0,this._error=void 0;try{this._schedule?await D(this.hass,this._schedule.id,C):await g1(this.hass,C),this._open=!1,this.dispatchEvent(new CustomEvent("schedule-plus-saved"))}catch(H){this._error=H instanceof Error?H.message:String(H)}finally{this._saving=!1}}render(){return this._open?t`
      <ha-dialog open @closed=${this._closeDialog}>
        <div class="form">
          <div class="dialog-title">
            ${this._schedule?"Edit schedule":"Add schedule"}
          </div>
          ${this._error?t`<div class="error">${this._error}</div>`:o}

          <label class="field-label" for="schedule-name">Name</label>
          <input
            id="schedule-name"
            type="text"
            class="native-input"
            .value=${this._name}
            @input=${C=>{this._name=C.target.value}}
          />

          <label class="field-label" for="device-type">Device type</label>
          <select
            id="device-type"
            class="native-select"
            .value=${this._deviceType}
            ?disabled=${this._schedule!==void 0}
            @change=${this._handleDeviceTypeChange}
          >
            ${P2.map(C=>t`<option value=${C}>${V1[C]}</option>`)}
          </select>

          <ha-formfield label="Enabled">
            <ha-switch
              .checked=${this._enabled}
              @change=${C=>{this._enabled=C.target.checked}}
            ></ha-switch>
          </ha-formfield>

          <label class="field-label" for="active-date-mode">Active period</label>
          <select
            id="active-date-mode"
            class="native-select"
            .value=${this._activeDateMode}
            @change=${this._handleActiveDateModeChange}
          >
            ${T1.map(C=>t`<option value=${C}>
                  ${C==="always"?"Always active":C==="include"?"Only during these date ranges":"Except during these date ranges"}
                </option>`)}
          </select>
          ${this._activeDateMode!=="always"?this._renderActivePeriodPanel():o}

          <label class="field-label">Entities</label>
          <scheduler-plus-entity-multi-picker
            .hass=${this.hass}
            .value=${this._entities}
            .domains=${k1[this._deviceType]}
            .includeEntities=${this.entityFilter}
            @value-changed=${C=>{this._entities=C.detail.value}}
          ></scheduler-plus-entity-multi-picker>

          <div class="rules-header">
            <label class="field-label">Rules</label>
            <button type="button" class="btn" @click=${this._openAddRuleDialog}>
              Add rule
            </button>
          </div>
          ${this._rules.length===0?t`<div class="placeholder">No rules yet.</div>`:t`
                <ul class="rules">
                  ${this._rules.map((C,H)=>this._renderRule(C,H))}
                </ul>
              `}

          ${this._conflicts.length>0?this._renderConflictPanel():o}

          <div class="dialog-actions">
            <button
              type="button"
              class="btn"
              ?disabled=${this._saving||this._checkingConflicts}
              @click=${this._openSaveAsTemplate}
            >
              Save as template
            </button>
            <span class="spacer"></span>
            <button
              type="button"
              class="btn"
              ?disabled=${this._saving||this._checkingConflicts}
              @click=${this._closeDialog}
            >
              Cancel
            </button>
            <button
              type="button"
              class="btn btn-primary"
              ?disabled=${this._saving||this._checkingConflicts}
              @click=${this._save}
            >
              ${this._checkingConflicts?"Checking\u2026":"Save"}
            </button>
          </div>
        </div>
      </ha-dialog>
      <scheduler-plus-rule-editor .hass=${this.hass}></scheduler-plus-rule-editor>
      <scheduler-plus-template-editor .hass=${this.hass}></scheduler-plus-template-editor>
    `:o}_renderConflictPanel(){return t`
      <div class="conflict-panel">
        <span class="conflict-title">
          This overlaps ${this._conflicts.length===1?"another schedule":"other schedules"}
        </span>
        <ul class="conflicts">
          ${this._conflicts.map(C=>t`
              <li class="conflict-row">
                <div class="conflict-info">
                  <span>${R1(C)}</span>
                  <span class="hint">${C.entity_ids.join(", ")}</span>
                </div>
                ${C.fixable?t`
                      <button
                        type="button"
                        class="btn"
                        @click=${()=>this._excludeConflict(C)}
                      >
                        Exclude "${C.conflicting_schedule_name}" on ${C.date}
                      </button>
                    `:t`<span class="hint">Adjust manually - can't auto-fix this one.</span>`}
              </li>
            `)}
        </ul>
        <button type="button" class="btn" @click=${this._saveAnyway}>Save anyway</button>
      </div>
    `}_renderActivePeriodPanel(){return t`
      <div class="filter-panel">
        <span class="panel-label">Date ranges</span>
        ${this._activeDateRanges.length===0?t`<span class="hint">None added yet.</span>`:t`
              <ul class="dates">
                ${this._activeDateRanges.map(([C,H],M)=>t`
                    <li class="date-row">
                      <span>${C} → ${H}</span>
                      <button
                        type="button"
                        class="btn"
                        @click=${()=>this._removeActiveDateRange(M)}
                      >
                        Remove
                      </button>
                    </li>
                  `)}
              </ul>
            `}
        <div class="range-add-row">
          <input
            type="date"
            class="native-input"
            .value=${this._newActiveRangeStart}
            @input=${C=>{this._newActiveRangeStart=C.target.value}}
          />
          <span class="sep">to</span>
          <input
            type="date"
            class="native-input"
            .value=${this._newActiveRangeEnd}
            @input=${C=>{this._newActiveRangeEnd=C.target.value}}
          />
          <button type="button" class="btn" @click=${this._addActiveDateRange}>
            Add range
          </button>
        </div>
      </div>
    `}_renderRule(C,H){let M=[...C.days].sort((d,A)=>b.indexOf(d)-b.indexOf(A)).map(d=>q[d].slice(0,3)).join(", "),e=[...C.dates.length>0?[`${C.dates.length} date${C.dates.length===1?"":"s"}`]:[],...C.date_ranges.length>0?[`${C.date_ranges.length} range${C.date_ranges.length===1?"":"s"}`]:[],...C.day_conditions.map(d=>m1[d])],i=e.length===0?"":C.date_mode==="exclude"?` \xB7 except ${e.join(", ")}`:C.date_mode==="include"?` \xB7 only ${e.join(", ")}`:"";return t`
      <li class="rule ${C.enabled?"":"disabled"}">
        <ha-switch
          .checked=${C.enabled}
          @change=${()=>this._toggleRuleEnabled(H)}
        ></ha-switch>
        <div class="rule-info">
          <span class="rule-name">${C.name}</span>
          <span class="rule-meta">
            ${M} · ${w5(C)}${i}
          </span>
        </div>
        <div class="row-actions">
          <ha-icon-button
            .path=${x1}
            label="Edit rule"
            @click=${()=>this._openEditRuleDialog(H)}
          ></ha-icon-button>
          <ha-icon-button
            .path=${E}
            label="Remove rule"
            @click=${()=>this._removeRule(H)}
          ></ha-icon-button>
        </div>
      </li>
    `}};c.styles=v`
    .form {
      display: flex;
      flex-direction: column;
      gap: 16px;
      min-width: 320px;
    }
    .dialog-title {
      font-size: 1.25rem;
      font-weight: 500;
      color: var(--primary-text-color);
    }
    .dialog-actions {
      display: flex;
      align-items: center;
      justify-content: flex-end;
      gap: 8px;
      padding-top: 8px;
      border-top: 1px solid var(--divider-color);
    }
    .spacer {
      flex: 1;
    }
    .btn {
      font: inherit;
      font-weight: 500;
      font-size: 14px;
      padding: 8px 16px;
      border-radius: 6px;
      border: 1px solid var(--divider-color);
      background: var(--card-background-color);
      color: var(--primary-text-color);
      cursor: pointer;
    }
    .btn:hover {
      background: var(--secondary-background-color, rgba(0, 0, 0, 0.06));
    }
    .btn:disabled {
      opacity: 0.5;
      cursor: default;
    }
    .btn-primary {
      background: var(--primary-color);
      border-color: var(--primary-color);
      color: var(--text-primary-color, #fff);
    }
    .btn-primary:hover {
      filter: brightness(0.95);
    }
    .error {
      color: var(--error-color);
    }
    .field-label {
      font-size: 0.85em;
      color: var(--secondary-text-color);
    }
    .native-select,
    .native-input {
      font: inherit;
      color: var(--primary-text-color);
      background: var(--card-background-color);
      border: 1px solid var(--divider-color);
      border-radius: 4px;
      padding: 8px;
    }
    .rules-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
    }
    .filter-panel {
      display: flex;
      flex-direction: column;
      gap: 8px;
      padding: 12px;
      border: 1px solid var(--divider-color);
      border-radius: 6px;
    }
    .conflict-panel {
      display: flex;
      flex-direction: column;
      gap: 8px;
      padding: 12px;
      border: 1px solid var(--warning-color, #ffa600);
      border-radius: 6px;
    }
    .conflict-title {
      font-weight: 500;
      color: var(--warning-color, #ffa600);
    }
    ul.conflicts {
      list-style: none;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      gap: 8px;
    }
    .conflict-row {
      display: flex;
      flex-direction: column;
      gap: 4px;
      padding-bottom: 8px;
      border-bottom: 1px solid var(--divider-color);
    }
    .conflict-row:last-child {
      border-bottom: none;
      padding-bottom: 0;
    }
    .conflict-info {
      display: flex;
      flex-direction: column;
      font-size: 0.9em;
    }
    .panel-label {
      font-size: 0.85em;
      font-weight: 500;
      color: var(--secondary-text-color);
    }
    .hint {
      font-size: 0.85em;
      color: var(--secondary-text-color);
    }
    ul.dates {
      list-style: none;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      gap: 4px;
    }
    .date-row {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 8px;
    }
    .range-add-row {
      display: flex;
      align-items: center;
      gap: 8px;
      flex-wrap: wrap;
    }
    .sep {
      font-size: 0.85em;
      color: var(--secondary-text-color);
    }
    .placeholder {
      padding: 8px 0;
      color: var(--secondary-text-color);
    }
    ul.rules {
      list-style: none;
      margin: 0;
      padding: 0;
    }
    .rule {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 8px 0;
      border-bottom: 1px solid var(--divider-color);
    }
    .rule ha-switch {
      flex: none;
    }
    .rule:last-child {
      border-bottom: none;
    }
    .rule.disabled .rule-name {
      color: var(--disabled-text-color);
    }
    .rule-info {
      display: flex;
      flex-direction: column;
      flex: 1;
      min-width: 0;
    }
    .rule-name {
      font-weight: 500;
    }
    .rule-meta {
      font-size: 0.85em;
      color: var(--secondary-text-color);
    }
    .row-actions {
      display: flex;
    }
  `,r([n({attribute:!1})],c.prototype,"hass",2),r([n({attribute:!1})],c.prototype,"entityFilter",2),r([a()],c.prototype,"_schedule",2),r([a()],c.prototype,"_open",2),r([a()],c.prototype,"_name",2),r([a()],c.prototype,"_deviceType",2),r([a()],c.prototype,"_enabled",2),r([a()],c.prototype,"_entities",2),r([a()],c.prototype,"_rules",2),r([a()],c.prototype,"_activeDateMode",2),r([a()],c.prototype,"_activeDateRanges",2),r([a()],c.prototype,"_newActiveRangeStart",2),r([a()],c.prototype,"_newActiveRangeEnd",2),r([a()],c.prototype,"_saving",2),r([a()],c.prototype,"_checkingConflicts",2),r([a()],c.prototype,"_conflicts",2),r([a()],c.prototype,"_error",2),r([O("scheduler-plus-rule-editor")],c.prototype,"_ruleEditor",2),r([O("scheduler-plus-template-editor")],c.prototype,"_templateEditor",2),c=r([x("scheduler-plus-schedule-editor")],c);function T5(){let L=new Date;return`${L.getFullYear()}-${String(L.getMonth()+1).padStart(2,"0")}-${String(L.getDate()).padStart(2,"0")}`}function j2(L){return!!L.override_until&&L.override_until>=T5()}function B5(L){if(L.device_type!=="climate")return;let V=new Set(L.rules.filter(C=>C.enabled&&C.on_enabled).map(C=>e1("climate",C.action)).filter(C=>C!==void 0));return V.size===1?[...V][0]:void 0}function P5(L){if(L.device_type!=="climate")return;let V=L.rules.filter(H=>H.enabled&&H.off_enabled);if(V.length===0||V.some(H=>!H.off_action))return;let C=new Set(V.map(H=>e1("climate",H.off_action)).filter(H=>H!==void 0));return C.size===1?[...C][0]:void 0}function R5(L){if(!L.next_event)return;let V=new Date(L.next_event);if(Number.isNaN(V.getTime()))return;let C=L.next_event_action==="off"?P5(L)??"Off":B5(L)??"On",H=V.toLocaleString(void 0,{weekday:"short",hour:"numeric",minute:"2-digit"});return`Next: ${C} ${H}`}function D5(L){if(L.active_now)return;if(!L.next_active_date)return"Inactive (outside its active dates)";let V=new Date(`${L.next_active_date}T00:00:00`);return Number.isNaN(V.getTime())?"Inactive (outside its active dates)":`Inactive until ${V.toLocaleDateString(void 0,{month:"short",day:"numeric"})}`}function F5(L){if(!L.override_pending_until)return;let V=new Date(L.override_pending_until),C=L.device_type==="climate"?(()=>{let M=new Set(L.rules.filter(e=>e.enabled&&e.on_enabled&&!e.allow_override).map(e=>e1("climate",e.action)).filter(e=>!!e));return M.size===1?` to ${[...M][0]}`:" to the scheduled setting"})():" to the scheduled setting";if(Number.isNaN(V.getTime()))return`Manual change${C} - reverting soon`;let H=V.toLocaleString(void 0,{weekday:"short",hour:"numeric",minute:"2-digit"});return`Manual change${C} - reverting ${H}`}function E5(L){if(!j2(L)||!L.override_until)return;let V=new Date(`${L.override_until}T00:00:00`);return Number.isNaN(V.getTime())?"Paused":`Paused through ${V.toLocaleDateString(void 0,{month:"short",day:"numeric"})}`}var $5="#F2A93B",S=class extends m{constructor(){super(...arguments);this._schedules=[];this._loading=!0;this._pendingToggle=new Set;this._toggleScheduleEnabled=async C=>{this._pendingToggle=new Set(this._pendingToggle).add(C.id);try{await D(this.hass,C.id,{...Q(C),enabled:!C.enabled}),await this._refresh()}catch(H){window.alert(H instanceof Error?H.message:String(H))}finally{let H=new Set(this._pendingToggle);H.delete(C.id),this._pendingToggle=H}};this._openAddDialog=()=>{this._editor?.showDialog()};this._openEditDialog=C=>{this._editor?.showDialog(C)};this._openDuplicateDialog=C=>{this._editor?.showDialogDuplicate(C)};this._openPauseDialog=C=>{this._overrideDialog?.showDialog(C)};this._resumeNow=async C=>{this._pendingToggle=new Set(this._pendingToggle).add(C.id);try{await D(this.hass,C.id,{...Q(C),override_until:null}),await this._refresh()}catch(H){window.alert(H instanceof Error?H.message:String(H))}finally{let H=new Set(this._pendingToggle);H.delete(C.id),this._pendingToggle=H}};this._openDayView=()=>{this._dayView?.showDialog()};this._openPreferences=()=>{this._preferences?.showDialog()};this._openQuickEvent=()=>{this._quickEventDialog?.showDialog()};this._openApplyTemplate=()=>{this._applyTemplateDialog?.showDialog()};this._handleUseTemplate=C=>{this._editor?.showDialogFromTemplate(C.detail.template)}}static getStubConfig(){return{type:"custom:scheduler-plus-card"}}static getConfigElement(){return document.createElement("scheduler-plus-card-editor")}setConfig(C){this._config=C}getCardSize(){return 2+this._visibleSchedules.length}get _visibleSchedules(){let C=this._config?.entities;return!C||C.length===0?this._schedules:this._schedules.filter(H=>H.entities.some(M=>C.includes(M)))}connectedCallback(){super.connectedCallback(),this._refresh()}async _refresh(){this._loading=!0;try{this._schedules=await h1(this.hass),this._error=void 0}catch(C){this._error=C instanceof Error?C.message:String(C)}finally{this._loading=!1}}async _handleDelete(C){window.confirm(`Delete schedule "${C.name}"?`)&&(await y2(this.hass,C.id),await this._refresh())}render(){return t`
      <ha-card>
        <div class="header">
          ${this._renderBrandMark()}
          <span>${this._config?.title??"Scheduler+"}</span>
          <ha-icon-button
            .path=${M2}
            label="My preferences"
            @click=${this._openPreferences}
          ></ha-icon-button>
          <ha-icon-button
            .path=${e2}
            label="Day view"
            @click=${this._openDayView}
          ></ha-icon-button>
          <ha-icon-button
            .path=${r2}
            label="Quick event"
            @click=${this._openQuickEvent}
          ></ha-icon-button>
          <ha-icon-button
            .path=${o2}
            label="From template"
            @click=${this._openApplyTemplate}
          ></ha-icon-button>
        </div>
        <div class="content">${this._renderContent()}</div>
        <div class="card-actions">
          <button type="button" class="btn btn-primary" @click=${this._openAddDialog}>
            Add schedule
          </button>
        </div>
      </ha-card>
      <scheduler-plus-schedule-editor
        .hass=${this.hass}
        .entityFilter=${this._config?.entities}
        @schedule-plus-saved=${this._refresh}
      ></scheduler-plus-schedule-editor>
      <scheduler-plus-day-view
        .hass=${this.hass}
        .entityFilter=${this._config?.entities}
      ></scheduler-plus-day-view>
      <scheduler-plus-preferences .hass=${this.hass}></scheduler-plus-preferences>
      <scheduler-plus-override-dialog
        .hass=${this.hass}
        @schedule-plus-saved=${this._refresh}
      ></scheduler-plus-override-dialog>
      <scheduler-plus-quick-event-dialog
        .hass=${this.hass}
        .entityFilter=${this._config?.entities}
        @schedule-plus-saved=${this._refresh}
      ></scheduler-plus-quick-event-dialog>
      <scheduler-plus-apply-template-dialog
        .hass=${this.hass}
        @scheduler-plus-use-template=${this._handleUseTemplate}
      ></scheduler-plus-apply-template-dialog>
    `}_renderBrandMark(){return t`
      <svg class="brand-mark" viewBox="0 0 60 60" aria-hidden="true">
        <rect
          x="9"
          y="13"
          width="34"
          height="14"
          rx="7"
          fill="var(--card-background-color)"
          stroke="var(--primary-text-color)"
          stroke-width="4"
        />
        <circle cx="37" cy="20" r="8.5" fill="var(--primary-text-color)" />
        <rect
          x="9"
          y="31"
          width="34"
          height="14"
          rx="7"
          fill="var(--card-background-color)"
          stroke="var(--primary-text-color)"
          stroke-width="4"
        />
        <circle cx="15" cy="38" r="8.5" fill="var(--primary-text-color)" />
        <circle
          cx="47"
          cy="47"
          r="12"
          fill=${$5}
          stroke="var(--card-background-color)"
          stroke-width="3.5"
        />
        <line
          x1="41"
          y1="47"
          x2="53"
          y2="47"
          stroke="var(--card-background-color)"
          stroke-width="3"
          stroke-linecap="round"
        />
        <line
          x1="47"
          y1="41"
          x2="47"
          y2="53"
          stroke="var(--card-background-color)"
          stroke-width="3"
          stroke-linecap="round"
        />
      </svg>
    `}_renderContent(){if(this._loading)return t`<div class="placeholder">Loading schedules…</div>`;if(this._error)return t`<div class="placeholder error">${this._error}</div>`;let C=this._visibleSchedules;if(C.length===0){let H=this._schedules.length===0?"No schedules yet.":"No schedules for this card's selected devices.";return t`<div class="placeholder">${H}</div>`}return t`
      <ul class="schedules">
        ${C.map(H=>this._renderSchedule(H))}
      </ul>
    `}_renderSchedule(C){let H=j2(C),M=C.enabled&&!H?R5(C):void 0,e=C.enabled?E5(C):void 0,i=C.enabled&&!H?D5(C):void 0,d=C.enabled?F5(C):void 0;return t`
      <li class="schedule ${C.enabled?"":"disabled"}">
        <ha-switch
          .checked=${C.enabled}
          ?disabled=${this._pendingToggle.has(C.id)}
          @change=${()=>this._toggleScheduleEnabled(C)}
        ></ha-switch>
        <div class="schedule-info">
          <span class="schedule-name">${C.name}</span>
          <span class="schedule-meta">
            ${V1[C.device_type]} ·
            ${C.entities.length}
            ${C.entities.length===1?"entity":"entities"} ·
            ${C.rules.length}
            ${C.rules.length===1?"rule":"rules"}
          </span>
          ${d?t`<span class="schedule-override-pending">${d}</span>`:o}
          ${e?t`<span class="schedule-paused">${e}</span>`:o}
          ${i?t`<span class="schedule-seasonal">${i}</span>`:o}
          ${M?t`<span class="schedule-next">${M}</span>`:o}
        </div>
        <div class="row-actions">
          <ha-icon-button
            .path=${x1}
            label="Edit"
            @click=${()=>this._openEditDialog(C)}
          ></ha-icon-button>
          <ha-icon-button
            .path=${t2}
            label="Duplicate"
            @click=${()=>this._openDuplicateDialog(C)}
          ></ha-icon-button>
          ${H?t`<ha-icon-button
                .path=${a2}
                label="Resume now"
                ?disabled=${this._pendingToggle.has(C.id)}
                @click=${()=>this._resumeNow(C)}
              ></ha-icon-button>`:t`<ha-icon-button
                .path=${i2}
                label="Pause"
                @click=${()=>this._openPauseDialog(C)}
              ></ha-icon-button>`}
          <ha-icon-button
            .path=${E}
            label="Delete"
            @click=${()=>this._handleDelete(C)}
          ></ha-icon-button>
        </div>
      </li>
    `}};S.styles=v`
    .header {
      display: flex;
      align-items: center;
      gap: 10px;
      padding: 16px 16px 0;
    }
    .brand-mark {
      width: 28px;
      height: 28px;
      flex: none;
    }
    .header span {
      flex: 1;
      min-width: 0;
      font-size: 1.5rem;
      font-weight: 500;
      line-height: 1.2;
      color: var(--ha-card-header-color, var(--primary-text-color));
    }
    .header ha-icon-button {
      flex: none;
    }
    .header ha-icon-button:last-child {
      margin-right: -8px;
    }
    .content {
      padding: 0 16px 16px;
    }
    .btn {
      font: inherit;
      font-weight: 500;
      font-size: 14px;
      padding: 8px 16px;
      border-radius: 6px;
      border: 1px solid var(--divider-color);
      background: var(--card-background-color);
      color: var(--primary-text-color);
      cursor: pointer;
    }
    .btn:hover {
      background: var(--secondary-background-color, rgba(0, 0, 0, 0.06));
    }
    .btn-primary {
      background: var(--primary-color);
      border-color: var(--primary-color);
      color: var(--text-primary-color, #fff);
    }
    .btn-primary:hover {
      filter: brightness(0.95);
    }
    .placeholder {
      padding: 16px 0;
      color: var(--secondary-text-color);
      text-align: center;
    }
    .placeholder.error {
      color: var(--error-color);
    }
    ul.schedules {
      list-style: none;
      margin: 0;
      padding: 0;
    }
    .schedule {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 8px 0;
      border-bottom: 1px solid var(--divider-color);
    }
    .schedule ha-switch {
      flex: none;
    }
    .schedule:last-child {
      border-bottom: none;
    }
    .schedule.disabled .schedule-name {
      color: var(--disabled-text-color);
    }
    .schedule-info {
      display: flex;
      flex-direction: column;
      flex: 1;
      min-width: 0;
    }
    .schedule-name {
      font-weight: 500;
    }
    .schedule-meta {
      font-size: 0.85em;
      color: var(--secondary-text-color);
    }
    .schedule-next {
      font-size: 0.85em;
      color: var(--primary-color);
    }
    .schedule-seasonal {
      font-size: 0.85em;
      color: var(--warning-color, #ffa600);
    }
    .schedule-paused {
      font-size: 0.85em;
      color: var(--error-color, #db4437);
    }
    .schedule-override-pending {
      font-size: 0.85em;
      font-weight: 500;
      color: var(--warning-color, #ffa600);
    }
    .row-actions {
      display: flex;
    }
    .card-actions {
      display: flex;
      justify-content: flex-end;
      padding: 8px 8px 8px 16px;
    }
  `,r([n({attribute:!1})],S.prototype,"hass",2),r([a()],S.prototype,"_config",2),r([a()],S.prototype,"_schedules",2),r([a()],S.prototype,"_loading",2),r([a()],S.prototype,"_error",2),r([a()],S.prototype,"_pendingToggle",2),r([O("scheduler-plus-schedule-editor")],S.prototype,"_editor",2),r([O("scheduler-plus-day-view")],S.prototype,"_dayView",2),r([O("scheduler-plus-preferences")],S.prototype,"_preferences",2),r([O("scheduler-plus-override-dialog")],S.prototype,"_overrideDialog",2),r([O("scheduler-plus-quick-event-dialog")],S.prototype,"_quickEventDialog",2),r([O("scheduler-plus-apply-template-dialog")],S.prototype,"_applyTemplateDialog",2),S=r([x("scheduler-plus-card")],S);window.customCards=window.customCards??[];window.customCards.push({type:"scheduler-plus-card",name:"Scheduler+",description:"Visual scheduling for lights and climate devices."});export{S as SchedulerPlusCard};
/*! Bundled license information:

@lit/reactive-element/css-tag.js:
  (**
   * @license
   * Copyright 2019 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

@lit/reactive-element/reactive-element.js:
  (**
   * @license
   * Copyright 2017 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

lit-html/lit-html.js:
  (**
   * @license
   * Copyright 2017 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

lit-element/lit-element.js:
  (**
   * @license
   * Copyright 2017 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

lit-html/is-server.js:
  (**
   * @license
   * Copyright 2022 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

@lit/reactive-element/decorators/custom-element.js:
  (**
   * @license
   * Copyright 2017 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

@lit/reactive-element/decorators/property.js:
  (**
   * @license
   * Copyright 2017 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

@lit/reactive-element/decorators/state.js:
  (**
   * @license
   * Copyright 2017 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

@lit/reactive-element/decorators/event-options.js:
  (**
   * @license
   * Copyright 2017 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

@lit/reactive-element/decorators/base.js:
  (**
   * @license
   * Copyright 2017 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

@lit/reactive-element/decorators/query.js:
  (**
   * @license
   * Copyright 2017 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

@lit/reactive-element/decorators/query-all.js:
  (**
   * @license
   * Copyright 2017 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

@lit/reactive-element/decorators/query-async.js:
  (**
   * @license
   * Copyright 2017 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

@lit/reactive-element/decorators/query-assigned-elements.js:
  (**
   * @license
   * Copyright 2021 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)

@lit/reactive-element/decorators/query-assigned-nodes.js:
  (**
   * @license
   * Copyright 2017 Google LLC
   * SPDX-License-Identifier: BSD-3-Clause
   *)
*/
